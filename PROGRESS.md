# PROGRESS — 회사↔집 인계 문서

## Last updated
2026-09-25 (**Phase 17 완료** — 사업비 입력 양식. evaluator PASS, 테스트 105파일 2,387건)

## Current goal
**사용자 추가 지시(2026-09-25) 4단계 진행 중 — SOT v4.7 확정.** ① Phase 15 성능(완료 154939f) → ② Phase 16 조직원·인건비·참여율(완료, schema 4) → ③ Phase 17 사업비 입력 양식(완료) → ④ **Phase 18 다크 모드**(진행). 결정: 참여율 합산은 **연단위 평균**, 급여 변경은 **과제별 [급여 반영]** 때만.

> ⚠️ **회사 PC는 `git pull` 후 반드시 `db push`.** Phase 13에서 3, Phase 16에서 **4**로 오른다.

> ⚠️ **HR API 키는 저장소에 없다.** 각자 `hr.unes.kr` 로그인 → 좌측 `🔑 API 키` → 용도 입력 → 회사 이메일 6자리 인증 → 발급 → 앱 `/settings` "사내 명부 연동"에 붙여넣는다. 키는 OS 키체인에만 저장되고 백업에 안 들어가므로 **PC마다 등록**한다(HR-11·HR-12). 2026-09-24 사용자가 발급한 키로 실호출 확인을 마쳤다(38명, 필드 8개 일치).
>
> ⚠️ **Supabase 무료 플랜은 7일 비활성이면 일시정지된다.** 2026-09-24에 한 달 만에 열었더니 DB·REST가 DNS부터 안 잡혔고, 대시보드에서 Resume한 뒤 정상으로 돌아왔다. 오래 쉬었다가 돌아오면 `npm test`가 `tenant/user not found`로 죽는데 **코드 문제가 아니다** — 대시보드에서 프로젝트를 먼저 깨운다.

> **수동 검증은 전체 개발이 끝난 뒤 한 번에 진행한다** (2026-08-08 사용자 결정). 아래 "수동 검증 > 대기"는 그때까지 쌓아 둔다. Phase 진행을 막지 않는다.

> ⚠️ **회사 PC는 `git pull` 후 반드시 `db push`를 해야 앱에 진입할 수 있다.** Phase 9에서 `schema_version`이 1 → 2로 올라가 §8.8 게이트가 걸린다. 의도된 안전장치다. (Phase 12는 스키마 변경이 없다.)

## Done this session
- **Phase 0 완료** (evaluator PASS 12/12, 커밋 07fa94a) — 스키마 25종+RLS+RPC, 리포지토리 16종, 테스트 48건
- **Phase 0.5 완료** (커밋 85df882→5154e67) — Tauri 셸+딥링크, 구글 OAuth 인증+승인 UI, 백업/복원. **실로그인 왕복 검증 완료** (브라우저·Tauri 양쪽)
- **Phase 1 완료** (evaluator 조건부 PASS, 테스트 232건) — 계층 + WBS:
  - 순수 함수: `lib/tree.ts`(WBS 코드·순환/깊이·날짜 롤업), `lib/progress.ts`(4단계 롤업), `lib/priority.ts`(긴급도·우선순위), `lib/dates.ts` — 부록 B.1(58 / 38.6667 / 59.1111)·B.0 수치 그대로 통과, P-8 중간 반올림 금지 회귀 테스트 포함
  - RPC 8종: `move_task`·`move_task_to_year`·`reorder_tasks`(순환 H-2·깊이 H-3·연차 H-1을 DB에서 차단), `create_project_with_defaults`(단계·연차·비목 12종 자동 생성)·`reorder_projects/stages/years`·`set_year_status`
  - 서버 액션 4종 + WBS 조회 모델(`getYearTree`/`getProjectFullTree`)
  - UI: 공통 프리미티브·`RealtimeRefresher`(R-1~R-7)·과제 목록/생성 → 과제 탭 10개·개요·단계연차 패널 → **WBS 트리 화면**(드래그·Tab/Shift+Tab·깊이 10 UI 차단·상세 패널)
  - O-3 충돌 처리: 상세 패널·인라인 이름 편집 모두 입력값 보존 + 항목별 비교/선택. 저장 payload 키 불변식은 **매핑 타입으로 컴파일 타임 강제**
- **Phase 2 완료** (evaluator PASS, 테스트 281건) — 인력 + 기관:
  - RPC 5종: `delete_member`(H-9 참조 8곳 정리 + 건수 반환), `count_member_references`, `set_lead_organization`(H-8 재지정 트랜잭션), `reorder_organizations`/`reorder_members`
  - `actions/team.ts` 액션 13종 + 조회 2종(getTeam, getTeamScreenData). H-8 삭제 차단·역할 강등 차단을 액션에서 수행
  - **소속 무결성**: createTask/updateTask/assignTaskMembers/bulkUpdateTasks가 남의 과제 인력·기관을 거부. 일괄 수정은 여러 과제 혼합 시 쓰기 전에 거부(부분 반영 방지)
  - UI: `/projects/[id]/team`(기관 카드·인력 기관별 그룹 테이블·PM 지정 경고·삭제 시 참조 8곳 건수·배정 작업 패널), WBS 트리 담당/기관 컬럼 + 필터, 상세 패널 담당자(단일+다중)·기관 편집(O-3 비교 대상 포함), 과제 개요 PM·주관기관 표시, 인력·기관 탭 활성화
- SOT 보강 4건: §8.3 X-2 definer 예외 목록, §6.1 P-9(예산 전부 0인 경우), 부록 A.3(과제·연차 상태 색상 + 색상 폴백), §6.6 H-8(주관 재지정 트랜잭션 규칙)
- **후속 정리 3건** (Phase 3 착수 전):
  - 파괴적 테스트 격리 — `tests/destructive/`로 분리(`npm run test:destructive`), 기본 `npm test`는 안전한 테스트만. 시작 전 실데이터 감지 가드가 테스트 소유가 아닌 행을 찾으면 시드조차 넣지 않고 거부한다
  - `createOrganization`: lead를 joint로 만든 뒤 `set_lead_organization`으로 승격 — 승격 RPC가 실패해도 lead가 2개로 남지 않는다 (H-8)
  - `updateProject`: `pmMemberId`·`leadOrgId`를 patch에서 제외(+`strict()`) — `setProjectPM`/`setLeadOrganization`의 가드를 우회할 수 없다

- **Phase 3 완료** (evaluator 조건부 PASS + 후속 조치 반영, 테스트 374건) — 목표 관리:
  - `lib/goals.ts` — §6.2 성과목표(D-1~D-5)·§6.3 기술목표(T-1~T-4) 계산. 부록 B.2 57.333…·B.3 수치 그대로 통과, **중간 반올림 시 57.4가 됨을 명시적으로 배제**하는 회귀 테스트 포함. direction 3종 × baseline 유무 6조합 + 경계값 전부 테스트(단위 47건)
  - RPC 2종: `reorder_deliverables`, `reorder_tech_targets`
  - `actions/goals.ts` 액션 16종 + `getGoalsData`(달성률은 저장하지 않고 `lib/goals.ts`로 계산). **N-13 과제 경계 검증**: targetByYear 키·achievement.yearId·record.yearId·orgId·memberIds가 남의 과제면 RULE 거부(FK가 못 막는 부분)
  - UI: `/projects/[id]/goals` 2탭(성과목표 테이블·연차 매트릭스 인라인 편집·실적 목록·유형별 도넛 / 기술목표 테이블·측정 이력·스파크라인·가중 달성률 게이지·인쇄 레이아웃), 개요 목표 요약 카드, 목표 탭 활성화
  - WBS 목표 연계: `linkTaskGoals` + createTask/updateTask/bulkUpdateTasks 목표 참조 경계 검증, 상세 패널 연계 편집(O-3 비교 대상 포함), 트리 연계 뱃지
- SOT 보강 7건 (누적): §8.3 X-2, §6.1 P-9, 부록 A.3 상태 색상, §6.6 H-8 재지정 트랜잭션, **§6.1 P-8 표시 형식(소수 1자리 고정)**, **§5.8·§5.9 자식 타입에 `version` 추가**(실적·측정 이력 편집이 O-1 대상인데 잠금을 걸 방법이 없던 모순 해소)

- **Phase 4 완료** (evaluator 조건부 PASS, 테스트 507건) — 마일스톤 + 대시보드:
  - `lib/dates.ts` 확장 — **기준일을 Asia/Seoul 달력 날짜로 고정**(`todayISO(now)`). 서버(사이드카, UTC일 수 있음)와 사용자 PC 시계가 달라도 같은 판정이 나온다. 테스트를 뉴욕 시간대로 돌려도 통과. `formatDday`, 마일스톤 overdue/upcoming 판정
  - `lib/risk.ts` — 리스크 등급(§6.5 Risk 행 + occurred 규칙), 테스트 67건
  - `generate_default_milestones` RPC — 연차평가·실적계획서 2건, **멱등**(재실행 시 추가 0건), 연차 종료일 없으면 조용히 오늘로 때우지 않고 명시적 거부
  - `actions/milestones.ts` 5종 + 조회, 과제 경계 검증(yearId·ownerMemberId)
  - 마일스톤 화면(연차 밴드 타임라인·목록·상태 인라인 변경·결과 메모), 개요 임박 마일스톤 카드, 연차 생성 시 기본 마일스톤 옵션(기본 해제)
  - **대시보드**(`/`) — §7.2 6종 전부: 지표 5개·과제 요약 카드·임박 마일스톤 타임라인·오늘 집중할 작업·주의 필요·오늘의 To-Do. 아카이브 과제는 전 집계에서 제외(단위 테스트로 고정). 벌크 조회(N+1 없음) + **Supabase 1000행 페이징 처리**(§12 5,000 작업이 조용히 잘리는 것 방지)
- SOT 보강 8건 (누적): …(이전 7건) + **§6.5 기준일을 Asia/Seoul 달력 오늘로 명문화**(로컬 타임존이면 화면마다 지연/정상이 달라짐)

- **Phase 5 완료** (evaluator 조건부 PASS·결함 없음, 테스트 580건) — 연구비:
  - `lib/budget.ts` — §6.4 집행률·B-1~B-3 판정·매트릭스 집계(항상 12행). 부록 B.4 수치 그대로 통과. **합계 집행률(94.3)이 개별 평균(90.93)이 아님**을 회귀 테스트로 배제, 중간 반올림 금지도 고정. Phase 4의 임시 `computeExecutionRate`를 이쪽으로 이전(정의 1곳)
  - `lib/currency.ts` — B-4 표시 환산(원 단위 저장, 표시만 천원/백만원). 대시보드의 로컬 사본 제거
  - **집행 내역 임베드 절단 대응** — PostgREST가 임베드 자식 1000행 초과를 **에러 없이 잘라서** 집행률이 조용히 틀리던 문제. 실측으로 확인(1,200건 중 1,000건만 반환) 후 부모 조회 + 자식 페이징 병합으로 교체. 1,200건 통합 테스트로 고정
  - `actions/budget.ts` 4종 + `getBudgetMatrix`. 현금/현물 합계 검증(차액을 서버가 임의 배분하지 않음 — DB 재조회로 확인)
  - 연구비 화면 — 12행 매트릭스(직접비/간접비 구분), 총액 인라인 편집, 집행 내역 패널(추가/수정/삭제), 하단 요약행(합계·집행률·잔액), B-1 경고 아이콘·B-2 빨강·B-3 배지, [엑셀 가져오기] 비활성(Phase 5.5 안내)
- SOT 보강 11건 (누적): …(이전 8건) + **§5.12 `BudgetExecution.version` 추가**(집행 편집도 O-1 대상), **§5.12 `amount` 0 이상 정수 명시**(실무에서 음수 없음 — 사용자 확인, 환불·감액은 원래 행 수정), **§9 `updateExecution`에 `expectedVersion?` 명기**

- **Phase 5.5 완료** (evaluator **PASS** — 비차단 지적 2건(문서 드리프트)은 즉시 반영, 테스트 843건) — 엑셀 예산계획 임포트. **SOT를 v3.3 → v3.4로 먼저 고치고 구현했다.**
  - **착수 전에 실측 샘플 2종을 프로토타입으로 직접 파싱해 SOT 규칙 공백 7건을 찾았다.** 머리로 검토했으면 전부 놓쳤을 것들이다:
    - **S-2'** carry-forward는 **판정에 성공한 라벨만** 승계 — 행안부 B열 세로쓰기 `직`/`접`/`비`가 아래 행으로 번져 **미매핑 3건**을 만들어냈다. S-7의 "우측 우선 매칭으로 자연히 무시된다"는 우측에 매칭될 라벨이 있을 때만 성립한다
    - **I-1 정규화 순서** 확정 — 각주 마커 `숫자)` 제거를 **괄호 제거보다 먼저**. `총 인건비1) (E=A+B+C+D)`의 `1)`은 짝 없는 닫는 괄호라, 순서가 뒤면 `총인건비1`이 남아 스킵되지 않고 **집계 행이 비목으로 반영**된다
    - **S-10** 정규화 후 빈 라벨 행의 분기(축 라벨이 있으면 직전 비목 승계) — 행안부 `연구재료비`/`(G)` 2행 분절에서 **현물 금액이 통째로 유실**되던 공백
    - **S-11** 병합 범위를 carry-forward보다 **먼저** 확장 — 산자부 `C16:E16` 가로 병합 메모가 위 행의 `현물` 축을 물려받아 **메모 행이 금액 행으로 둔갑**하던 문제
    - **S-12** 라벨 판정을 우→좌 스캔 + 첫 판정 채택으로 알고리즘화. 라벨 없이 금액만 있는 행은 `건너뜀(라벨 없음)`으로 미리보기에 남긴다
    - **S-13** 시트 추천을 비목 매칭 **행 수 → 행 비율**로 교체 — 워크북에 연차별 산출근거 시트(240~250행)가 같이 들어 있어 **행 수로 매기면 두 파일 모두 총괄표가 아닌 시트를 추천**했다 (산자부 0.655 vs 0.139, 행안부 0.379 vs 0.135)
    - **`CASH_INKIND_AXIS`**(부록 C) — `일반`/`통합관리`가 `unassigned`(plannedAmount에만 합산)라는 규칙이 주석에만 있어 구현이 임의로 현금에 몰아넣을 수 있었다. 상수로 승격
  - **부록 B.6 신설** — 실측 2종의 1차년도 기대 파싱값 + 구조 자동 감지 기대 동작. 통합 테스트 기준값
  - `lib/import/` 순수 함수 9모듈(xlsx 무의존) — normalize·categorize·amount·grid·structure·matrix·preview. 부록 B.5 10행 시나리오를 픽스처로 그대로 통과
  - `lib/import-adapter.ts` — SheetJS → `RawSheet` 어댑터. `import 'server-only'`로 I-13을 컴파일 타임에 못 박음
  - `commit_import`/`restore_import_snapshot` RPC(security invoker) + `lib/db/import-snapshots.ts`. 단일 트랜잭션·과제 경계 검증·I-17 스냅샷(과제별 20개 창)
  - `actions/import.ts` — `runImportPipeline`이 preview·commit의 **유일한** 파싱 경로다(§9: 미리보기와 반영이 달라선 안 된다). `fileHash` 대조, `blocked` 재계산 금지
  - `components/budget/import/` 5단계 마법사 + 툴바 [엑셀 가져오기] 활성화
  - **결과: 실측 2종 모두 미매핑 0건.** 파싱 결과의 직접비 합·총액이 원본 `직접비 소계`·`연구개발비 총액` 행과 **원 단위까지 일치**(산자부 296,510,000 / 298,510,000, 행안부 133,340,000 / 133,340,000)
  - SOT 보강 (누적 11 → **20건**): 위 7건 + I-17 보강(20개 창은 과제별·복원은 스냅샷을 만들지 않고 행을 지우지 않음) + §7.9.1 Step 5 `신규`/`덮어씀` 판정 기준(행 존재가 아니라 **값의 존재**) + Step 4 아이콘 근거를 서버가 싣는다 + 계약 공백 4건(`yearMapping` 키 = 엑셀 열 문자 / `commitImport`의 `profileId`는 별도 인자 / `orientation='column'` 명시적 거부 / `ImportProfile`에 데이터 끝 행을 저장하지 않는 이유) + 부록 B.5 표 열 문자 정정 + §10 디렉터리 구조 갱신

- **Phase 6 완료** (evaluator **PASS**, 테스트 898건) — 리스크 + 노트. SOT v3.4 → **v3.5**:
  - **리스크** (`actions/risks.ts`, `components/risks/`, `reorder_risks` RPC): 5×5 히트맵(셀 클릭 필터)·목록 9컬럼·행 확장(내용/대응/비상/WBS 링크)·해결·종료 기본 숨김 토글. 점수·등급·색상은 전부 `lib/risk.ts`(Phase 4, 67건)에서만 나온다 — 경계값 8·15가 다른 곳에 복제되지 않은 것을 evaluator가 grep으로 확인
    - 히트맵 셀이 **미해결/해결 id를 나눠 담아** "해결·종료 표시" 토글이 셀 개수와 목록 건수를 구조적으로 함께 움직인다(두 필터가 따로 놀 여지가 없다)
    - §6.5 `occurred`: 해결·종료는 등급 판정 대상이 아니라 `severity=null`("판정 제외"), 미해결 `occurred`는 점수와 무관하게 "주의"
  - **노트** (`actions/notes.ts`, `lib/notes.ts`, `components/notes/`): 2단 레이아웃·고정 상단→날짜 내림차순·필터 4종(유형/연차/태그/전문검색)·편집/분할/미리보기 토글·회의록 템플릿·참석자 다중 선택·Task/Milestone 연결 + **역참조**(WBS 상세 패널·마일스톤 펼친 행)·명시적 저장 + 3초 디바운스 자동 저장
    - **마크다운 렌더링에 외부 라이브러리를 쓰지 않는다.** 노트 본문은 사용자 입력이라 HTML 문자열을 만들어 주입하면 저장형 XSS다. `lib/notes.ts`가 제한된 부분집합을 **데이터 AST**로 파싱하고 뷰어가 **React 엘리먼트**로 옮긴다 — AST에 "원시 HTML" 노드가 없어 주입이 걸러지는 게 아니라 **표현 자체가 불가능**하다. `dangerouslySetInnerHTML` 사용 0건(evaluator grep 확인), 새 npm 의존성 0개. 안전하지 않은 링크(`javascript:`·`data:`·`vbscript:`·`//host`)는 조용히 버리지 않고 원문 그대로 표시. **이 규칙은 SOT §7.12와 CLAUDE.md에 못 박아 뒀다**
    - 자동 저장 × O-1: 저장 성공 시 baseline version을 갱신해 **자기 저장에 대한 가짜 STALE**을 막는다(Phase 1 후속 개선 ②에서 겪은 문제를 반복하지 않음). 삭제 중에는 자동 저장을 멈추고, `updateNote`는 upsert가 아니라 update 전용이라 **뒤늦은 자동 저장이 삭제된 노트를 되살리지 못한다** — 둘 다 통합 테스트로 고정
  - **과제 개요(§7.3) 자리표시 제거** — 고위험 리스크 5건·최근 노트 5건 카드를 실제 데이터로 채웠다. 정렬·등급은 각각 `getRiskMatrix`/`sortNotes`가 계산한 값을 그대로 쓴다(개요와 전용 화면이 다른 순서를 보이지 않게)
  - 탭 활성화: `TabNav.IMPLEMENTED_THROUGH = 6`
  - SOT 보강: **§9 `reorderRisks(projectId, orderedIds)` 정정**(과제 소속 reorder는 전부 컨테이너를 받는데 리스크만 빠져 있었다 — 없으면 RPC가 과제 경계를 검증할 수 없다), §7.12 마크다운 렌더링 방식 명문화, §10 갱신, **§9 조회 블록 드리프트 경고**(아래 참조)

- **Phase 7 완료** (evaluator **PASS**, 테스트 944건) — 간트 + 칸반. SOT v3.5 → **v3.6**:
  - **간트** (`lib/gantt.ts` 30건, `actions/gantt.ts`, `components/gantt/`): 좌측 연차>트리 패널, 일/주/월 스케일(1일 = 24/8/3px), 막대 내 진척률 채움, 부모 얇은 요약 막대(양끝 캡), 연차 배경 밴드 + 경계 세로선, 마일스톤 상단 마름모 레인(유형별 색), 오늘 세로선, 막대 드래그 이동·양끝 리사이즈
    - 좌표는 전부 `lib/gantt.ts` 순수 함수다. `날짜차이 × px`가 컴포넌트에 복제되지 않았다
    - **롤업된 부모 막대는 끌 수 없다**(P-14~P-16: 자식에서 계산된 값이라 저장할 곳이 없다). 커서·툴팁으로 알린다
    - 드래그 저장은 O-1. 실패 시 **원위치 + ErrorBanner**(조용히 되돌리지 않음), STALE이면 위치를 유지한 채 비교 배너
    - **잘라내거나 숨기지 않는다**: 연차 밴드가 겹치거나 기간이 없으면 `overlappingIds`/`unplottableIds`로 돌려 화면 하단에 사실을 적는다. 월 스케일에서 최소 폭(8px)이 적용된 막대는 점선 아웃라인 + "최소 폭으로 넓게 그린 막대 N건" 안내
  - **칸반 + 매트릭스** (`lib/board.ts` 16건, `actions/board.ts`, `components/board/`): 뷰 전환 토글, 연차 필터 공유, 4컬럼 칸반(컬럼 간 드래그 → status, 컬럼 내 → order), 리프 기본 + 전체 토글, 담당자 스윔레인, 좌측 등급 색 띠 / 5×5 매트릭스(가로 긴급도·세로 중요도), 셀 클릭 필터, 우측 점수 내림차순 목록
    - **§7.6의 "§7.11과 같은 컴포넌트를 공유한다"를 실제로 이행** — `components/ui/Matrix5x5.tsx`로 추출하고 리스크 히트맵이 그것을 쓰게 했다. 리스크 쪽 공개 API가 그대로라 `RiskScreen.tsx`는 한 줄도 안 고쳤고, 통합 테스트 16건 회귀 통과
    - **PR-4 준수**: 셀 드롭은 **중요도만** 저장한다. 긴급도는 마감일 기반이라 `urgencyMode='manual'`로 고정된 작업에서만 바뀌고, `auto`인 작업은 **조용히 무시하지 않고** 그 사실을 안내한다
    - **X-3 준수**: `reorderTasks`는 컨테이너 자식 전체를 요구하므로 서버가 컨테이너 목록을 함께 내려준다(화면 필터로 형제가 빠져 order가 깨지는 것 방지). 다른 상위 작업 사이로 끌면 계층 변경이라 저장하지 않고 "WBS 트리에서 옮기세요"로 거절
  - 탭 활성화: `TabNav.IMPLEMENTED_THROUGH = 7` — 10개 탭 중 **9개가 열렸다**(설정 화면만 Phase 8)
  - **SOT §7.5 결정 보류 명시**: "접힘 상태를 WBS 화면과 동기화"에 **저장 위치가 없다**(localStorage / URL 쿼리 / 사용자 설정 테이블). 셋 다 장단이 뚜렷해 사람이 정할 문제라 지어내지 않았다 — 결정 전까지 **화면별 로컬 상태**
  - 미구현 1건: 카드의 "담당자 아바타"는 Member에 사진 필드가 없어(§5.11) **이름 + `+N` 뱃지**로 치환

- **후속 정리 2건** (PROGRESS 대기 목록에서 처리):
  - `TechRecordForm.tsx` 측정일 기본값을 로컬 `new Date()` → **Asia/Seoul**(`todayISO`). 타임존이 다른 PC에서 하루 어긋난 측정일이 저장되던 문제
  - **테스트 중단 시 dev DB 잔여물 자동 청소** — `tests/global-setup.ts`(실행 1회). `wbs-test+%` 중 2시간 이상 경과분만. **삭제 순서가 핵심**: 사용자를 지우기 전에 소유 행을 먼저 지운다(auth.users를 먼저 지우면 `created_by`가 set null이 되어 그때부터 실데이터와 구분 불가). 3시간 전으로 백데이트한 가짜 잔여물을 심어 감지·삭제·로그를 실제로 확인

- **Phase 8 완료** (evaluator **PASS**, 테스트 **997건**)
  - **Tauri 번들 실검증 완료** — 이 세션에서 Rust 툴체인을 처음 설치했다(rustup + VS Build Tools 2022). `src-tauri/target/`이 아예 없었다 = **Rust 쪽이 이 저장소에서 한 번도 컴파일된 적 없었다**는 뜻이었는데, 컴파일이 4분 03초에 통과하고 `WBS_0.1.0_x64_en-US.msi`(54MB)·`WBS_0.1.0_x64-setup.exe`(36MB) 2종이 나왔다. 사이드카 실파일 2,050개 + `node.exe`가 WiX/NSIS 설치 스크립트에 **정확히 같은 수로** 반영된 것까지 evaluator가 대조 확인
  - **그 과정에서 SOT 모순 1건 발견 → §14.1 정정**: 배포 형태가 `.msi`로 적혀 있었는데 MSI는 `perMachine`이라 관리자 권한을 요구한다. §12 "관리자 권한 없이 사용자 폴더에 설치"와 정면 충돌 — 실제로 번들을 만들어 보기 전에는 드러날 수 없던 모순이다. 배포 산출물을 **NSIS setup.exe 하나로** 못박았다 — To-Do + 설정 + 마감. SOT v3.6 → **v3.7**:
  - **착수 전에 SOT를 먼저 고쳤다.** §7.13이 두 줄짜리 스텁이라 구현이 임의로 정해야 하는 지점이 4곳이었다 — `오늘` 필터의 정의, 지연 판정, 정렬 tie-break, 우선순위 색. 물어보고 **T-D1~T-D10**으로 명문화한 뒤 구현했다
  - **To-Do** (`lib/todos.ts` 34건 · `actions/todos.ts` · `app/todos/` · `components/todos/` 5종 · `reorder_todos` RPC): 한 줄 빠른 추가, 행 인라인 편집, 필터 4종, 정렬 3종, 수동 드래그
    - **T-D3 정의 단일화** — `lib/dashboard.ts`에 인라인으로 박혀 있던 "오늘의 To-Do" 필터·정렬을 `lib/todos.ts`로 이관하고 대시보드가 그것을 호출한다. 정의가 두 벌이면 `/todos`와 대시보드의 건수가 조용히 어긋난다. **기존 대시보드 테스트가 수정 없이 통과**하는 것으로 동작 불변을 고정
    - **T-D6 의도된 차이** — 대시보드는 아카이브 과제를 제외하지만(집계 화면) `/todos`는 포함한다(To-Do의 유일한 접근 경로 — 여기서 감추면 되살릴 방법이 없다). 양쪽에 근거 주석
    - **O-1/O-2 분리** — `toggleTodo`는 `expectedVersion`을 아예 받지 않고(O-2: 사용자가 만진 필드 1개), `updateTodo`는 필수로 받는다(O-1). patch 스키마에 `done`이 없어 O-1 경로로 우회 갱신이 불가능하다
    - **T-D10** — 필터가 걸린 채 드래그해도 **숨겨진 항목까지 포함한 전체 순서**를 보낸다. 덕분에 `reorder_todos` RPC가 "배열 길이 = 갱신 행 수"를 엄격히 검사할 수 있고, 존재하지 않는 id·중복을 조용히 넘기지 않는다(통합 테스트로 고정)
  - **설정 §7.14 3섹션 완성** — 팀 설정 폼(§5.16 6필드, `schemaVersion`은 patch에서 제외 + `.strict()`로 N-10 트리거보다 먼저 거부) / 사용자 관리 + **내 프로필**(액션은 Phase 0.5부터 있었고 UI만 없었다) / 백업·복원 + **임포트 스냅샷 목록·복원**
    - `restore_import_snapshot`을 감싸는 **서버 액션이 없던 숙제 해소**. 통합 테스트의 복원 부분도 리포지토리 직접 호출 → 액션 경유로 전환(기대값 무수정 통과)
  - **인쇄 3종 (§12 P-R1~P-R5 신설)** — `@page`는 셀렉터를 못 받는다. 이름 붙은 페이지(`page:`)는 Chromium 전용이라 Safari에서 **경고 없이 세로로 떨어진다**(=동작하는 척). 그래서 globals.css에 기본 세로를 깔고, 가로가 필요한 화면만 `PrintHeader`가 마운트 중에만 `<style media="print">`를 head 끝에 붙였다 뗀다. 예산 매트릭스·리스크 대장 가로 / 기술목표표 세로
    - 흑백 대비(P-R5): 집행률 100% 초과 셀에 `초과` 글자, 리스크 등급 뱃지의 흰 글자를 인쇄에서 검정 테두리로
  - **§12 계산 성능 검증** — 5,000 노드(루트 75·최대 깊이 5·리프 2,898) 합성 → `computeProgressMap` **중앙값 0.80~0.89ms** (임계 100ms). 통과할 때도 실측 ms를 `process.stderr.write`로 남겨 회귀가 눈에 보인다
  - SOT 보강 (누적 20 → **28건**): T-D1~T-D10, §7.13 **소유권 = 팀 공유** 명문화("개인"은 *나만 본다*가 아니라 *과제 계층에 안 매인다*는 뜻 — RLS를 좁히면 K-7 전체 대체 복원이 깨진다), §12 검증 방법 표, §12 P-R1~P-R5, 부록 A.3 Todo 색 3종, §7.5 접힘 상태 **보류를 "v1에서 안 한다"로 마감**

- **Phase 9 SOT 작성 완료** (2026-08-08, SOT v3.7 → **v4.0**) — 코드는 아직 없다. 착수 전 결정 4건을 사용자가 확정했고 실측 워크북의 **산출근거 시트**를 재대조해 스펙을 썼다:
  - **결정 4건** — ① 산출근거가 있으면 **내역 합계가 비목 총액을 확정**(셀 편집 잠금) ② 범위는 **내역 편집 + 지침 검증까지** (산출근거 엑셀 임포트·제출 서식 내보내기는 범위 밖) ③ 인건비 인력은 **Member 확장 + 내역이 참조** ④ 연구비 화면 안 **[제안 | 수행] 모드 토글**
  - **인건비 보안 제약 없음** (사용자 확인) — 우리 팀이 원래 인건비를 다루는 팀이다. 마스킹·열람 제어를 두지 않는다. §5.11에 명문화
  - 신설: **§5.17 BudgetDetail**(비목 → 세목 → 산출 행, PL-D1~D6), **§6.10 계산·집계·검증**(PL-1~PL-16), **부록 A.5 세목 프리셋**(33종), **부록 B.7 계산 검증 예시**
  - 개정: §5.12 계획액 소유권(PL-9 잠금 / PL-10 트랜잭션 불변식), §5.11 Member `annualSalary`·`hireType`, §5.3 Project 한도 2종, §6.8 **S-14**(임포트가 잠긴 셀을 덮지 않는다), §7.9 모드 토글 + §7.9.2 산출근거 패널, §9 액션 5종 + 조회 2종, §11 Phase 9 행
  - **§13 미결정 1번(참여연구원 참여율) 해소** — Member가 아니라 `BudgetDetail`이 갖는다. 한 사람이 한 연차에서 참여율을 바꿔 두 구간이 되는 실측 사례(행안부 한봄희 4개월 10% + 8개월 53%)가 Member 단일 필드로는 표현되지 않는다
  - **부록 B.7은 실측 값으로 검증했다** — 산자부 1차년도 인건비 19행이 `연봉 × 참여율/100 × 개월/12 + 조정액`으로 합계 현금 180,840,000 / 현물 88,650,000까지 정확히 맞고, 간접비 비율 0.9622%가 서식 셀과 소수 4자리 일치. **PL-2 회귀**: 월액(`연봉/12`)을 먼저 반올림하면 박선욱이 15,540,001이 되어 1원 어긋난다
  - **원본 서식의 조정액 열은 참고 표시라 두 행(유태일·김형식)에서 천원 단위로 절사돼 있다** — 그대로 옮기면 500원·250원 어긋난다. SOT는 `조정액 = 최종 − 산식결과`로 정의하고 역산값을 실었다. 이 사실을 부록 B.7에 기록
  - `lib/budget-plan.ts`를 `lib/budget.ts`와 **합치지 않는다** — 방향이 반대인 두 규칙이 한 파일에 있으면 기준액 정의가 섞인다

- **Phase 9 완료** (evaluator **PASS**, 테스트 **54 파일 / 1,154건**) — 예산 제안 모드:
  - `lib/budget-plan.ts` — §6.10 산출근거 금액(PL-1~PL-5)·집계(PL-6~PL-8)·지침 검증(PL-11~PL-15)·축 비중(`computeAxisSplit`). **부록 B.7 수치를 DB 왕복으로 통과**: 현금 180,840,000 / 현물 88,650,000 / 계 269,490,000, 활동비 27,020,000, E1 269,490,000, 간접비 기준액 207,860,000, **간접비 비율 0.9622%**, 총액 298,510,000
  - **PL-2 회귀** — 월액(`연봉/12`)을 먼저 반올림하면 박선욱이 15,540,001이 되는 경로를 명시적으로 배제. 두 경로가 갈리는 3행(박선욱·신나리·장선우)까지 고정
  - **PL-11 ↔ PL-13 비대칭 회귀** — E1은 `personnel_support` 세목을 **빼고**, 간접비 기준액은 **넣는다**. 실측 샘플은 C=0이라 부록 B.7로 안 잡히므로 `personnel_support > 0` 케이스를 따로 만들었다(잘못된 비목 단위 합산이 내는 E1 180,000,000과 "경고가 안 뜬다"는 사실까지 단언)
  - `budget_details` 테이블 + RLS + publication, RPC 4종. **PL-10a — RPC에 금액 산식이 한 줄도 없다**(`sum(amount) filter (where axis=…)`가 전부). `pg_proc.prosrc` 정규식 검사로 실측 확인
  - **PL-10 트랜잭션 불변식** — 산출근거를 건드리면 같은 트랜잭션에서 `budget_items` 3금액 재계산. 셀 이동 시 떠난 셀·도착한 셀 **둘 다**. 무변경이면 UPDATE 자체를 건너뛴다(헛된 `version` 증가 = 남의 낙관적 잠금 파괴 방지)
  - **PL-9 잠금** — 산출근거가 있는 셀은 계획액 편집 차단(매트릭스·집행 패널·서버 액션 3중). 마지막 행을 지우면 **직전 합계가 남고** 잠금 이유만 `detail` → `split`으로 바뀐다
  - **PL-10b 연봉 파급** — `previewSalaryChange`로 영향 건수·전후 금액을 보여주고 확인받은 뒤 `apply_salary_change` 단일 RPC. 영향 0건이면 확인 없이 저장
  - **H-9a** — 인건비 산출근거가 걸린 Member는 삭제 차단(`on delete restrict` + RPC 사전 거부 + 화면 버튼 차단 + `[연구비로 이동]`). `active=false`는 그대로 허용
  - **S-14** — 총괄표 임포트가 잠긴 셀을 덮지 않는다. `잠김`은 `건너뜀`과 **별개 상태**이고 `blocked`에 포함되지 않는다(오류가 아니므로 반영을 막지 않는다). 사용자의 명시적 건너뛰기가 잠금보다 우선
  - UI: 연구비 화면 `[제안 | 수행]` 토글(화면 로컬 상태, 탭 증가 없음), 산출근거 패널(세목 섹션·`personnel`/`quantity` 컬럼 분기·금액 읽기 전용·드래그 정렬·O-3 입력 보존), 지침 검증 줄(현금/현물 비중 포함 — **미입력이 있으면 비율 대신 그 사실을 밝힌다**), 인력 화면 연봉·`채용예정` 배지, 과제 개요 지침 한도 카드
  - **부록 B.6 등가 확인** — 같은 과제에 2차년도를 총괄표 임포트로 넣고 산출근거로 쌓은 1차년도와 `BudgetRuleEvaluation` 객체 전체를 `toEqual` 대조. 두 경로의 차이는 편집 가능성뿐
  - **§8.8 `schema_version` 1 → 2** + 올리는 기준 명문화("백업 파일 형식이 바뀌는가"). `budget_details`가 Phase 0 이후 첫 테이블 추가라 백업 형식이 바뀌었다
  - **K-7 파괴적 왕복 검증 완료** (2026-08-09, 7/7) — 전 행 삭제 후 복원이 `budget_details`까지 되살린다. 이때 **테스트 시드 결함**을 하나 잡았다: `postgres` 드라이버는 jsonb 파라미터로 가는 **JS 문자열을 한 번 더 JSON 인코딩**해서 배열이 아니라 **jsonb 문자열 스칼라**를 저장한다(읽기 전용 프로브로 실측). `${'[...]'}::jsonb` → `${sql.json([...])}::jsonb`로 고쳤다. **프로덕션 영향 없음** — 쓰기는 전부 supabase-js → `upsert_budget_detail`을 거치고 `postgres` 드라이버는 테스트 전용이다. 같은 패턴이 다른 곳에 없음을 확인했다
- SOT 보강 (v3.7 → **v4.0**): §5.17·§6.10·부록 A.5·부록 B.7 신설, §5.12(계획액 소유권)·§5.11·§5.3·§6.8 S-14·§7.9/§7.9.2·§9·§11 개정, **§6.6 H-9a**·**§6.10.2 PL-10a/PL-10b**·**§8.8 버전 기준** 추가. **§13 미결정 1번(참여연구원 참여율) 해소**

- **Phase 10 SOT 작성 완료** (2026-08-09, SOT v4.0 → **v4.1**) — 코드는 아직 없다. 실측 워크북 2종의 **산출근거 시트를 구조 분석**해 썼다:
  - **결정 2건** — ① 임포트 대상 셀에 기존 산출근거가 있으면 **기본 건너뜀 + 셀별 `[기존 삭제 후 교체]` 선택**(조용히 덮지 않는다) ② 성명이 명부에 없으면 **마법사에서 새 인력 생성 허용**(연봉·직위를 파일에서)
  - 신설: **§6.11**(D-1~D-21), **§7.9.3 마법사 4단계**, **부록 C.2 세목 별칭**, **부록 B.8 실측 기준값**. `ImportKind`에 `'budget_detail'` 추가, §9 액션 3종
  - **구조는 두 부처가 거의 동일**하다: 섹션(`1. 직접비 소요명세`) → 비목(`- 인건비`·`다. 연구시설·장비비`…) → 세목(`①`~`⑪`) → 컬럼 헤더 → 데이터 → `소계`. **컬럼 형태는 7가지로 수렴**하고 인건비만 부처별 열 위치가 다르다 → **위치가 아니라 헤더 텍스트로 매핑**(D-7)
  - **놓치기 쉬운 함정 4개를 규칙으로 못 박았다**: ① 간접비 섹션의 `가./나./다.`는 비목이 아니라 세목이다 — `다.`가 직접비에선 `연구시설·장비비`, 간접비에선 `성과활용지원비`다(D-2) ② 인건비 소계가 **3단**이라 첫 소계에서 멈추면 신규채용 34,000,000을 통째로 놓친다(D-5) ③ 섹션 밖 총괄표(1~59행)를 읽으면 **이중 계상**이다(D-1) ④ 실측 행안부에 **`합계($)`** 열이 있다 — 통화 확인 게이트(D-10)
  - **D-8 조정액 흡수** — 파일의 `합계` 열을 최종 금액으로 삼고 산식과의 차액을 `adjustment`에 넣는다. 부록 B.7.1의 조정액 열이 곧 임포트의 기대 산출물이라, **임포트 결과가 원본 서식과 원 단위까지 일치**한다
  - **D-14** — 임포트가 `members.annual_salary`를 고치지 않는다. PL-10b가 확인을 요구하는 값을 임포트가 우회하면 다른 연차 금액까지 말없이 흔든다
  - **`lib/import/` 파이프라인을 그대로 재사용**한다(I-1 정규화·I-8~I-12 금액·S-11 병합·부록 C 별칭). 새로 필요한 건 행 구조 해석뿐이다

- **Phase 10 완료** (2026-08-10, evaluator **PASS**, 테스트 **단위 1,054 + 통합 349**) — 산출근거 시트 임포트:
  - `lib/import/detail-sheet.ts`(구조 감지·행 변환)·`detail-preview.ts`(성명 매칭·조정액 확정·셀 충돌)·`actions/detail-import.ts`(액션 3종)·`commit_detail_import` RPC·4단계 마법사(§7.9.3). **`lib/import/` 파이프라인을 그대로 재사용**했고 새 파서를 만들지 않았다
  - **부록 B.8을 실측 워크북으로 통과** — 인건비 현금 180,840,000 / 현물 88,650,000 / 계 269,490,000, 활동비 27,020,000, 총액 **298,510,000**
  - **B.7 ↔ B.8 등가 확정** — 같은 파일에서 손 입력 경로와 임포트 경로를 **두 과제에 각각 실제로 돌린 뒤** `budget_items` 12비목을 원 단위로 대조. 상수만 공유하면 "기대값 표가 같다"만 증명되고 *경로가 같은 곳에 도달한다*는 증명이 안 된다
  - **PL-10a 유지** — `commit_detail_import` RPC에 금액 산식이 한 줄도 없다. `amount`는 서버 액션이 `lib/budget-plan.ts`로 계산해 넘긴다
  - **D-8a** — 조정액을 **명부 연봉** 기준으로 확정한다. 파일 연봉으로 역산하면 저장값과 재계산값이 어긋나 첫 편집에 금액이 말없이 바뀐다. 명부 연봉이 null이면 **0원 + 경고**(조정액에 금액 전부를 넣으면 나중에 연봉을 채우는 순간 두 배가 된다)
- **실측에서만 드러난 파서 결함 10건을 잡았다** (합성 픽스처 961건은 전부 통과하는데 실물에서 13건 실패). 이것이 `tests/unit/import-detail-samples.test.ts`를 만든 이유다:
  - **D-24** 비목 헤더 행에 금액이 있으면 비목이 통째로 미감지 → 시설·장비비가 인건비로 계상될 뻔했다(실측은 0원이라 총액이 안 흔들렸다)
  - **D-25** 세로 병합 행이 복제돼 출장비 1,200,000 → 2,400,000, 총액 299,710,000
  - **D-7** 연봉 열 미매핑 — `실지급액\n(연봉)`을 I-1이 괄호를 지워 `실지급액`으로 만든다. `formulaAmount=0` → **조정액이 금액 전액**을 떠안아 D-8이 무의미해진다
  - **D-22** 엑셀 백분율 서식 셀은 저장값이 100배 작다(`10.0%` → `0.1`). `RawCell.percentFormat` 신설 + **어댑터에 `cellNF: true`** — 이게 없으면 `cell.z`가 안 만들어져 판정이 영영 false이고 **실제 임포트에서만** 인건비가 1/100이 된다
  - 그 밖에 D-7a(메모 열이 헤더 판정을 깨뜨림)·D-23(비목 합계 줄이 앞/같은/다음 행에 다 나타남)·헤더 3행 결합
- **SOT 보강 (v4.0 → v4.1, 구현 중 12개 규칙 추가)**: §6.11 D-1~D-25, §7.9.3, 부록 B.8·C.2 신설. 구현하며 드러난 공백을 그때그때 메웠다 — **D-3a**(세목 헤더 없는 비목) **D-7a D-8a D-15a**(커밋 시 셀 충돌) **D-15b**(교체인데 행 없음) **D-17a**(복원이 산출근거 행을 지운다 — I-17의 명시적 예외) **D-17b**(복원 후 인력은 남는다) **D-22~D-25**

- **후속 정리 4건 완료** (2026-08-10, 테스트 **63파일 1,417건**):
  - **C1 마법사 후속** — `withColumnRoleOverride`가 그 블록의 `axisOverrides`를 함께 비운다. 안 비우면 Step 2에서 열 역할을 바꾼 순간 그 행이 D-9로 갈리며 `axisSuggested=false`가 되어 **서버가 매번 막는데 셀렉트가 사라져 화면에서 지울 통로가 없다** — 사용자가 갇힌다. 블록 행 범위로 정확히 가려내되 **어느 블록에도 안 걸리는 키는 전부 버린다**(그런 키는 앞으로 어떤 조작으로도 지워지지 않는다). 순수 함수 3종을 `detail-wizard-state.ts`로 모으고, D-9 거부 경로 통합 테스트 추가
  - **C2 SOT §9 이름 정리** — 조회 21종을 실제 이름·시그니처·**쓰는 화면**까지 표로 맞췄다. 드리프트 경고를 "2026-08-10 정리 완료"로 교체. §10 `actions/` 목록도 실제 구조로 정정
  - **C3 빌드·테스트 위생** — `prebuild`가 `.next`를 비운다(`cache`는 보존). dev가 남기는 잔여물은 **해시 없는 이름**이라 디렉터리 필터로는 가장 큰 것(`chunks/app/layout.js` 3.67MB)을 못 잡고, 무엇보다 필터는 **빌드 자체의 ENOENT 산발 실패**를 못 고친다. `prepare-sidecar.mjs`는 dev 잔여물을 발견하면 **조용히 거르지 않고 exit 1**로 멈춘다(그런 `.next`는 산출물을 믿을 수 없다)
  - **C4 연구비 이중 조회** — `getBudgetMatrix`가 매트릭스를 만든 **그 배열**을 `items`로 함께 내린다. `BudgetScreen`의 `items` prop을 없애 **서로 다른 시점의 두 스냅샷을 짝지어 넘기는 일 자체를 타입에서 제거**했다. 조회 3회 → 2회. 집행 내역 페이징(1000행 절단 방어)은 그대로
  - **테스트 잔여물 표식** — `destroyTestUser`가 **소유자를 끊기 직전에** 고정 UUID 센티널을 라벨 컬럼에 찍는다. "만들 때"가 아니라 "지우기 직전"인 것이 핵심이다 — 그 시점에 `created_by`가 테스트 사용자임이 SQL로 증명된 상태라 **실데이터에 이 표식이 붙을 경로가 구조적으로 없다.** 삭제 술어는 표식 + `created_by is null` + 2시간 경과 3중 AND
  - **죽은 액션 제거** — `listPendingUsers()`는 선언만 있고 호출자가 없었다. §7.14는 `listAppUsers`로 전 사용자를 읽고 대기자를 상단에 올린다(승인 직후 그 사람이 목록에서 사라지지 않아야 조작 결과가 눈에 남는다). SOT §9에서도 뺐고, `listBackups()`가 서버 액션이 아닌 이유(백업은 DB 행이 아니라 사용자 폴더의 파일)도 명기

- **Phase 11 완료** (2026-08-12, evaluator **PASS**, 테스트 **70파일 1,592건** + 파괴적 7/7) — 제출 서식 엑셀 내보내기. SOT v4.2 §6.12 개정:
  - **처음부터 만들지 않고 값을 비운 템플릿에 값만 쓴다**(X-1). `templates/산출근거_표준.xlsx`는 실측 산자부 워크북에서 값만 비운 것이고, 만드는 절차를 `scripts/build-template.mjs`로 저장소에 남겼다(`samples/`가 gitignore라 CI에는 못 걸지만, 부처가 늘 때 손작업을 기억으로 반복하지 않게)
  - **착수하자마자 X-4의 전제가 틀렸음이 드러났다.** 서식의 **행 단위 금액 열이 수식**이고(`J65 = TRUNC(G65*H65*I65,-3)`) 그 수식 7개에 **원본 과제의 조정액이 상수로 박혀** 있었다(`…,-3)-270000`). 그 줄에 다른 인력을 쓰면 남의 조정액이 따라붙어 금액이 조용히 어긋난다. 사용자 결정으로 **행 금액은 값으로 덮어쓰고 블록 소계·총계 `SUM`은 살린다**(X-4/X-4a) — 검산은 그대로 남는다
  - **서식의 실제 금액 모델을 알아냈다**: `TRUNC(연봉 × 참여율 × 개월, -3) + 조정상수`. 6행 전부 원 단위로 맞는다. **부록 B.7의 조정액 설명이 틀렸던 것을 바로잡았다** — "참고 표시라 천원 단위로 절사"가 아니라 수식 안의 상수이고 나머지는 `TRUNC`가 흡수한다. 최종 금액은 두 모델이 같고 **분해만 다르다**. PL-1에 `TRUNC`를 넣을지는 **지금 결정하지 않는다**(바꾸면 B.7·B.8 기준값과 Phase 9·10 테스트가 전부 흔들리는데 최종 금액이 같아 얻는 것이 없다)
  - **총괄표의 연차 열은 집계·비율까지 전부 값으로 쓴다**(X-10a·X-10c). 그 셀들이 총괄표 안의 SUM이 아니라 `산출근거!G21` 같은 **시트 간 참조**인데 산출근거에는 내보내는 연차 **하나만** 들어가기 때문이다 — 수식을 살려 두면 2차년도 값이 1차년도 칸에 찍힌다. 앱에 데이터가 없는 괄호 메모 행(통합관리비·연구실 안전관리비)은 **지어내지 않고 비운 뒤 알린다**(X-10d, §6.11 I-5가 임포트에서 건너뛰는 줄과 대칭). 비율 행의 합계 열은 **연차별 비율의 합**이 되므로 비운다(X-10e)
  - **X-5a 신설** — `checkCapacity`가 **인자 개수만** 세서, 실측 `나. 연구지원비` 표(단가 열도 인자 열도 없음)에 들어가는 `단가 2,000,000 / 인자 0개` 행이 **경고 없이 근거를 통째로 잃었다.** 인자가 1개라도 있으면 잡히는데 0개면 빠져나가는 종류의 구멍이다
  - **⭐ 왕복이 `budget_details`까지 닫혔다** — 손 입력 과제 → 내보내기 → 재임포트(`inspect → preview → commit`) → **다른 과제**의 `budget_details`·`budget_items` 12비목이 원 단위로 일치. 현금 180,840,000 / 현물 88,650,000 / 총액 **298,510,000**. 두 경로가 서로를 모르므로 "기대값 표가 같다"가 아니라 *경로가 같은 곳에 도달한다*는 증명이다
  - **SheetJS는 수식을 재계산하지 않는다** — X-4a 검산을 위해 살아남은 수식 183개를 **직접 파싱해 계산**하는 최소 계산기를 테스트에 뒀다(`SUM`·`TRUNC`·`ROUND`만 존재함을 실측 확인). **모르는 함수를 만나면 던진다** — 조용히 건너뛰면 검산이 사라진다
  - **템플릿을 만들며 내가 낸 결함 3건을 실측이 잡았다**: ① 세로 병합 구간 라벨(`C65:C87 = 기존인력`·`C89:C90 = 신규채용`)을 성명 옆에 있다고 데이터로 오인해 지웠다 → 제출 서식의 `인력구분` 칸이 빈 채 나갔다 ② 인자 라벨(`회`·`월`·`직급`)이 한 글자라 구조 패턴에 안 걸려 지워졌다 ③ 시트명만 바꾸고 수식 참조를 안 고쳐 총괄표 전 열이 `#REF!`가 될 뻔했다(→ `validateLayout`의 `dangling-sheet-ref`로 상설 그물화)
  - `next.config.ts`에 `outputFileTracingIncludes` 추가 — 템플릿은 런타임 fs 읽기라 **standalone 산출물에서 통째로 빠진다.** Tauri 사이드카가 그걸 읽으므로 놓쳤으면 배포에서만 터졌다
  - `lib/export/**`는 **SheetJS 무의존**(`lib/import/`와 같은 경계, 정적 스캔 테스트), 어댑터는 `server-only`, `.next/static`에 SheetJS 흔적 0건
  - SOT 보강: §6.12 **X-4/X-4a/X-4b · X-5a · X-10a~X-10e** 신설·개정, 부록 B.7 조정액 설명 정정, §13 17번에 **부처 규정 제약** 기록(아래 참조). `evaluation_criteria.md`의 낡은 X-4 문구도 개정판으로 맞췄다

- **Phase 12 SOT 작성 완료** (2026-08-12, SOT v4.2 → **v4.3**) — 코드는 아직 없다. 사용자가 HR API 문서(`hr.unes.kr/api/external/guide`)를 주고 **`GET /api/external/users`를 쓰라**고 지정했다. 명세를 실제로 읽고 썼다:
  - **⚠️ 가장 중요한 발견 — API에 연봉이 없다**(HR-2). 응답 필드는 8개(`user_id`·`user_email`·`user_name`·`user_division`·`user_team`·`user_position`·`user_role`·`user_is_active`)뿐이고 문서가 "비밀번호·연차·전화번호 등 민감 필드는 제외"라고 명시한다. 입·퇴사일도 없다. **연동 지점을 `annualSalary`로 적어 둔 이전 기록이 틀렸다** — 채울 수 있는 건 `이름·직위·이메일` 3개고, 인건비 산출근거의 손품은 줄지 않는다. §7.10.1에 "연봉이 비어 있다는 사실을 화면에 남긴다"를 규칙으로 넣었다
  - **결정 2건 (사용자)** — ① 본부·팀은 **저장하지 않고 고르는 화면에만 표시**(동명이인 구분용). `orgId`는 컨소시엄 참여기관이라 사내 본부와 개념이 달라 섞으면 주관/공동/위탁 판정이 오염된다 ② API 키는 **사용자별 발급 + OS 키체인**. 공용 키를 사이드카에 실으면 앱을 가진 누구나 전 직원 명부를 조회할 수 있다(절대 규칙 1과 같은 이유)
  - **HR-5 — `user_is_active`로 `Member.active`를 바꾸지 않는다.** HR의 비활성은 **퇴사**, 우리 것은 **그 과제의 참여 종료**다. 자동으로 맞추면 인사 이동이 협의 끝난 예산의 인력 구성을 말없이 흔든다. 대신 `퇴사` 배지로 표시만 하고 선택은 막지 않는다(지난 연차 예산에 이름이 남아야 하는 경우가 있다)
  - **동기화가 아니다** — 자동 실행 없음(HR-7), 기존 Member 덮어쓰기 없음(HR-8), 명부 캐시 없음(HR-17). 가져오기는 **새 Member 생성만** 한다. 매칭 키는 `user_email`(HR 문서 권장, 불변)이고 `user_id`는 우리 쪽에 저장하지 않는다(저장하면 동기화 의무가 생기는데 지지 않기로 했다)
  - **실패를 감추지 않는다**(HR-14~HR-16) — `401` 키 폐기 / `403` **허용 IP 아님**(HR 서버 `EXTERNAL_ALLOWED_IPS`) / `429` 15분당 300회 초과(**자동 재시도 금지** — 한도를 더 빨리 태운다) / `500`·`503`. 필드가 빠진 항목은 버리지 않고 `읽을 수 없음`으로 남긴다
  - 신설: **§6.13**(HR-1~HR-17), **§7.10.1**(인력 화면 `[사내 명부에서 추가]`), §7.14 API 키 섹션, §9 액션 2종(`fetchHrDirectory`·`createMembersFromHr`), §11 Phase 12 행, §13 18·19번
  - `lib/hr.ts`는 **fetch를 하지 않는다** — `lib/import/`·`lib/export/`와 같은 경계. 네트워크는 서버 액션의 몫

- **Phase 12 완료** (2026-09-24, evaluator **PASS**, 테스트 **74파일 1,675건**) — 사내 인사 명부 연동. **타이핑을 줄이는 것이 전부다** — 자동 동기화·명부 복제·재직 상태 반영·연봉 채움 없음:
  - **착수 전에 HR 가이드를 실제로 읽고 실호출로 확인했다** (가이드는 `/api/external/guide?t=<키>`로 발급 키가 있어야 열린다): 인증 헤더는 **`X-API-Key` 하나**(Bearer 아님), 실패 코드 `401 INVALID_API_KEY`/`403 IP_NOT_ALLOWED`/`503 NOT_CONFIGURED`/`429`(15분당 300회, `/api/external/*`+`/api/auth` 합산). **실측 명부 38명 중 `user_division`이 null인 사람이 1명** — 문서(string)와 달라 HR-1을 `string|null`로 고쳤다. 표시 전용 필드라 `읽을 수 없음`으로 막으면 그 사람을 못 고르게 된다
  - SOT 보강 3건(코드보다 먼저): HR-1 `user_division` null 허용 / §9 `fetchHrDirectory(apiKey, projectId | null)` — 설정의 [연결 확인]은 과제 맥락이 없어 `null`이면 `이미 등록됨` 판정 생략 / §9 인증 헤더·실패 코드·**타임아웃 15초** 명기(없으면 HR이 응답을 안 줄 때 버튼이 영원히 "불러오는 중")
  - `lib/hr.ts` 순수 함수(fetch·supabase·next 무의존, 단위 39건): `parseHrUsers`(HR-16 — success≠true·users 비배열은 실패값, 결손 항목은 `malformed[]`에 사유와 함께, `count` 불일치 플래그) · `toMemberDraft`(HR-4 — 키 3개를 `Object.keys`로 고정) · `markSelectable`(HR-8·HR-9 — 이메일 trim+소문자 정규화, `이미 등록됨`/`이메일 없음`은 선택 불가, **퇴사는 배지만**) · `assembleDirectory`(unreadable을 원 위치에 끼움) · `formatHrError`(HR-14 — code·message 둘 다 포함, 403에 허용 IP 언급)
  - `lib/hr-key.ts` — 키체인 항목 `wbs.hr-api-key`. Tauri가 아니면 **모듈 변수(세션 메모리)** 에 두고 `storage:'session'`으로 알린다(조용한 no-op 아님). `maskApiKey`는 끝 4자리만. localStorage·DB·env 어디에도 안 간다
  - `actions/team.ts` 2종: `fetchHrDirectory`(키는 인자로만, 로그·에러문·반환값에 안 실림 — 통합 테스트가 반환값을 직렬화해 키 부재 확인 / 429 재시도 없음 / `이미 등록됨` 판정은 **서버가** 기존 인력을 읽어 수행) · `createMembersFromHr`(`strict()`로 division/team/`user_is_active`/`annualSalary` 거부, 리포지토리 경유, 기존 이메일은 **그 항목만** `rejected[]`로 돌리고 나머지 생성, 중간 실패 시 "N명은 이미 생성됐습니다" 명시). 통합 25건 — fetch는 `hr.unes.kr`만 가로채는 라우터로 스텁(Supabase가 같은 전역 fetch를 쓴다)
  - `tests/unit/hr-boundary.test.ts` 정적 경계 검사: lib/hr.ts 무의존 / 'use client' 파일에 `fetch(`+`hr.unes.kr` 공존 금지 / team.ts에 `retry`·`console.*apiKey` 없음 / HR 5파일에 웹 스토리지 없음 / **산출물 5개 존재 단언**(skipIf가 조용히 꺼지는 것 방지)
  - UI: `/settings` "사내 명부 연동"(`HrApiKeyPanel` — password 입력, 저장 후 끝 4자리, [연결 확인]은 클릭에서만 HR 호출, [키 삭제], 발급 안내 + "백업에 안 들어가 PC마다 등록" 문장, 비Tauri "이 창에서만 유지됩니다") · 인력 화면 `[사내 명부에서 추가]`(`HrDirectoryModal` — 키 없으면 안내+`/settings` 링크, 5컬럼+검색, 선택 불가 행은 회색·사유·disabled로 **남긴다**, 퇴사 배지는 선택 가능, count 불일치 상단 표시, 닫으면 명부 폐기) → 추가 후 notice **"N명을 추가했습니다. 연봉은 사내 명부에 없어 비어 있습니다 — 인건비 산출근거를 쓰려면 채워야 합니다"**(HR-2)
  - `evaluation_criteria.md`에 Phase 12 체크리스트를 SOT에서 옮겨 신설(착수 시점에 없었다)

- **Phase 13 SOT 작성 완료** (2026-09-24, **코드 없음**) — 연구비 사용 규칙. SOT v4.3 → **v4.4**:
  - 사용자가 확보한 고시 2종(과기부 제2026-38호 50쪽·기후부 제2026-29호 116쪽)을 pypdf로 텍스트 추출 → 에이전트 2개가 수치 조항 **144건**(73+71)을 조문과 함께 표로 정리(스크래치 `rules-msit.md`·`rules-moe.md`, 저장소 밖). 그중 **우리 데이터로 판정 가능한 19건**만 규칙(RL-3~RL-19)으로, 나머지는 부록 D.3 안내 목록으로
  - §5.18 `BudgetRule`(규칙 = 데이터: code·enabled·value·base·severity·source·note, 출처 빈 문자열 금지) / §6.14 RL-1~RL-19(분모 정의를 문서에 고정) / §7.9 규칙 검증 패널 + §7.9.5 편집 패널 / §9 액션 4종 / §11 Phase 13 행 / 부록 B.9 검증 예시(B.7 실측 + 합성) / 부록 D 프리셋 2종(값마다 조문)
  - **발견한 불일치**: PL-13 간접비 분모가 고시보다 좁았다(6비목 현금 합 — promotion·other 누락). 과기부와 기후부의 수정직접비 정의가 서로 다르다(위탁·부담비 제외 여부) → `base`로 해결
  - 텍스트 추출 한계: 계산식·별표 6(간접비고시비율표)이 뽑히지 않았고, 기후부 제23조③ 현금 비율표의 원천/혁신제품 열 구분이 사라졌다. 부록 D 끝에 "사람이 원문으로 확인할 것"으로 적어 둠

- **토스 디자인 시스템 토큰 이식 완료** (2026-09-24, 사용자 선택 A — 토큰 + 프리미티브 + 전 화면 색상 치환). SOT v4.4 → **v4.5**, 부록 E 신설:
  - `@toss/tds-mobile`은 React 16~18 + emotion 요구(우리는 React 19), 라이선스 미표기 → **컴포넌트 라이브러리는 쓰지 않고 값만 이식**. `app/globals.css` `@theme`에 TDS 8스케일 × 10단계 + 시맨틱(screen·surface·hairline·dimmed) + 타이포 t1~t7 + 둥글기. **Tailwind 기본 팔레트 제거**(`--color-*: initial`) — 옛 이름이 남으면 색이 빠져 눈에 띈다
  - 폰트 Pretendard Variable(OFL, npm `pretendard`) 번들 포함(Tauri 오프라인)
  - 치환: `slate→grey`, `rose→red`, `emerald→green`, `amber→orange`, `sky→blue`, `indigo·violet→purple` — 117파일 2,273곳(app·components·lib·tests·SOT 부록 A.3). Badge의 tone 키 이름(`amber`·`violet`)은 API라 그대로 두고 클래스만 바꿈
  - 프리미티브: Button(파란 주 버튼, 테두리 없는 회색 보조), Badge(rounded-md, t7), Modal(rounded-3xl, dimmed), ErrorBanner, ProgressBar. body `bg-screen font-sans`
  - `tests/unit/design-tokens.test.ts`가 옛 팔레트 사용을 금지하고 globals.css 토큰을 고정. 단위 1,286건·tsc·build 통과
  - **하지 않은 것**: 레이아웃·동작 변경 없음. 화면별 톤 다듬기(카드 여백·제목 위계 t-scale 적용)는 이후 Phase에서 손대는 화면마다. 다크 모드 없음

- **Phase 13 완료** (2026-09-24, evaluator **PASS**, 테스트 **82파일 1,848건**) — 연구비 사용 규칙. 계획은 `docs/plans/phase-13-plan.md`(T1~T9) 그대로 실행:
  - **스키마 v3**: `budget_rules`(check 제약으로 RL-D2·D3·D5를 DB가 강제, RLS, `(project_id, code)` 유일) + Phase 9 한도 컬럼 2종을 규칙 행으로 **이관 후 삭제**(마커 블록을 통합 테스트가 재실행해 2/1/0행 고정) + `apply_rule_preset` RPC(단일 트랜잭션, fill/overwrite, 프리셋 밖 행·메모 보존) + `restore_backup` 재정의. dev DB에 적용됨
  - `lib/rules.ts` 판정기(RL-3~RL-19, 단위 71건) + `lib/rules-presets.ts`(부록 D 2종, **source 타입 강제** `과기부고시 …|기후부고시 …`) + `lib/budget-plan.ts` **PL-13 분모 정정**(직접비 11비목 현금 − base별 제외; promotion 현금 1,000,000 케이스에서 옛 분모와 달라짐을 테스트로 고정). 부록 B.9 수치(0.9622 / 69.2308 / 11.35 / 44,500,000 / 35,000,000 / 21,000,000 합산 / 29,990,000 통과·30,000,000 warn) 그대로 통과. 원값 비교(한도 69.23에 69.2308은 error)
  - 액션 4종(`actions/budget-rules.ts`) + `getBudgetPlanData`가 `RuleEvaluation`을 싣는다(저장 안 함). `setBudgetRateLimits`·`BudgetRateLimitCard` 삭제. 내보내기(Phase 11) 전 확인에 findings가 notice로 실리고 **blockers에는 절대 안 들어간다**(error여도 내보내기 성공 — 통합 테스트)
  - UI: 제안 모드 하단 **규칙 검증 패널**(비율 표 7종 — 꺼져도 표시, findings 클릭 → 연차 열 강조/산출근거 행 강조, skipped 접힌 목록, "근사" 배지, 0건 안내) + 툴바 **[연구비 규칙]** 편집 모달(프리셋 채우기/덮어쓰기 2단계 + 건수, 인라인 편집·O-3 비교, gov_share 75/67 셀렉트+직접 입력, 삭제 확인, D.3 안내 목록 읽기 전용). 수행 모드에는 없음
  - **구현 중 SOT에 더한 것**: §6.14.6 `skipped.yearId`·detail scope `category`(화면이 코드에서 비목을 추측하지 않도록 판정기가 싣는다), `actual`은 원값, §9 overwrite 범위·Realtime 제외, §6.10.4 시그니처 `evaluateBudgetRules(yearTotals, indirectBase)`, `lib/constants.ts` `INDIRECT_BASE_LABELS`(분모 라벨 1곳)
  - 미결로 남긴 것(블로킹 아님): `apply_rule_preset`의 note null = 메모 유지 규약 / `ActionResult`에 NOT_FOUND 코드가 없어 없는 과제는 code 없이 "찾을 수 없습니다"(기존 관례) / 내보내기의 행 단위 finding은 내보내는 연차만 싣는다(화면 패널과 건수가 다를 수 있음 — X-9 취지)

- **Phase 14 완료** (2026-09-25, evaluator **PASS**, 테스트 **88파일 2,064건**) — 도움말 + 따라하기. SOT v4.5 → **v4.6**(§7.16·§7.17·§5.16·§7.3 [과제 삭제]·§7.14 5섹션). 스키마 변경 없음:
  - **도움말 `/help`**: `content/help/*.md` 17편(화면 15 + 계산 방식 + FAQ)을 서버가 fs로 읽어 **노트와 같은 AST 렌더**(`lib/notes.ts` → `MarkdownViewer blocks`). 화면 5곳 헤더 `?` → `/help#<slug>`(과제 탭은 경로로 slug 결정). 본문 수치는 SOT·부록 D 조문 병기(HP-3) — evaluator가 8편 표본 대조로 지어낸 것 없음 확인. `next.config.ts` `outputFileTracingIncludes`에 `./content/**`(standalone 확인 17/9). 테스트가 slug 목록·머리 형식·표/상대 링크 0·3,500자를 고정
  - **따라하기**: 우측 아래 `[따라하기]` → 드로어 9단계(`lib/tutorial.ts` 레지스트리 + `content/tutorial/*.md`). `[예제 과제 만들기]`(`createSampleProject`)는 **기존 액션만 순서대로** 불러 `[예제] 따라하기 예제 과제`(연차 2·기관 2·인력 4·작업 8·목표 3·마일스톤·산출근거 6행·규칙 프리셋)를 만든다 — 부분 실패 시 만든 id와 함께 실패 반환(확장 반환형). 완료 판정은 `getTutorialStatus`가 데이터로(⑦은 산출근거 AND 규칙), 내보내기·백업만 수동 체크. 상태는 `LocalConfig.tutorial`(PC별, Zod `.catch`). 삭제는 **신설된 과제 개요 `[과제 삭제]`**(2단계: 연쇄 안내 → 과제명 입력)
  - 통합 테스트: 7단계 판정을 데이터 하나씩 넣으며 고정, 예제 생성 후 PL-10·H-8 불변식 확인, 9단계 실패 mock으로 부분 실패 경로
  - 예제 인건비 조합은 `existing_cash_le_new`를 스스로 어기지 않게 골랐다(기존 현금 22.2M ≤ 신규 27M). `existing_personnel_cash` warn 2건은 의도된 예제(현금 3행 요구)

- **Phase 16 완료** (2026-09-25, evaluator **PASS**, 테스트 **96파일 2,195건**) — 조직원 · 인건비 · 참여율. 계획 `docs/plans/phase-16-plan.md`(T1~T13):
  - **스키마 v4**: `staff`(이메일 `lower(trim)` 유일)·`staff_salaries`(적용일 유일, basis·amount check, 퇴직금·4대보험 플래그) + `members` 4컬럼(staff_id set null + 스냅샷 3). `apply_salary_change`에 `p_snapshot` 인자 — 연봉과 기준 스냅샷이 **같은 UPDATE**. `restore_backup`·`RESTORE_TABLES`에 staff 2종(projects 앞). dev DB 적용됨
  - 순수 함수 `lib/salary.ts`(SL-1 환산·SL-2 적용 이력·배지)·`lib/participation.ts`(PS-1~PS-4 연단위 합산 — 110 error·95 warn·90 ok·100 warn, 원값 비교, 미연결·연도 미정 카운트, student·아카이브 제외). **산식은 PL-1 하나** — 새 파일에 연봉 곱셈 없음(정적 검사)
  - 액션: `actions/staff.ts` 12종(급여 이력 CRUD는 Member를 **건드리지 않는다** — SL-5 정적 검사), `actions/team.ts` `linkMemberToStaff`·`previewStaffSalaryApply`·`applyStaffSalary`(PL-10b 경로 재사용)·`fetchHrDirectoryForStaff`, 수동 연봉 수정 시 스냅샷 null
  - 화면: `/staff`(목록·현재 급여·배지·상세 패널 급여 이력 CRUD·참여율 매트릭스·사내 명부 추가·삭제 2단계), 인력 화면 `조직원` 열·[연결](이메일 자동 제안·조직원으로 등록)·[급여 반영](연차 기준 미리보기)·연봉 기준 배지, 제안 모드 `[매트릭스 | 인건비]` 토글 — 인건비 탭(조직원 × 월급 × 참여율 × 개월, 인라인 편집, E1, 다른 과제 포함 계상률)
  - 비차단 관찰(후속): 다른 조직원으로 재연결해도 이전 스냅샷이 남는다(SOT 미결정) / `/staff`의 `?` 도움말 slug가 임시로 settings / 상세 패널 "현재" 배지 기준일이 클라이언트 시계

- **Phase 17 완료** (2026-09-25, evaluator **PASS**, 테스트 **105파일 2,387건**) — 사업비 입력 양식. 계획 `docs/plans/phase-17-plan.md`:
  - `lib/input-form/`(좌표 맵 = 데이터, `_meta` 숨김 시트, 파서는 memberId·`비목:세목` 복합 키로만 연결, 금액 열은 읽지 않음, 미리보기 diff·커밋 페이로드) + `lib/input-form-adapter.ts`(server-only, 수식 셀). **새 RPC 없음** — 기존 `commit_detail_import` 재사용
  - 인건비 시트 수식 `ROUND(연봉×참여율/100×개월/12,0)`(월액 셀 미참조 — PL-2), 테스트 안 수식 계산기로 19행 검산. 부록 B.7 왕복 총액 298,510,000 원 단위 일치. 금액 셀을 999로 덮어써도 서버 값
  - **통합 테스트가 결함을 잡음**: 반영 시점에 교체 비목을 재판정하면 미리보기 뒤 끼어든 행이 삭제되고 D-15a가 절대 발동하지 않았다 → SOT IN-6 개정(교체 목록은 미리보기 시점, 클라이언트가 넘김). 그 외 SOT 보강: IN-4 복합 키(`default` 세목이 6비목 공유), IN-5 비목 단위 교체·추가만 있는 비목 제외·`category-moved` 차단
  - 화면: 제안 모드 툴바 `[입력 양식 내려받기]`(연차 선택)·`[입력 양식 올리기]`(파일 → 미리보기: 추가/변경/삭제/유지 건수·비목 합계·경고·삭제될 행 → 반영). `blocked`만 막고 경고는 안 막음
  - 기록용 소견: 알 수 없는 인력·세목 행은 파일째 차단(조용히 빠지지 않게), 빈 축은 현금 기본 + 알림

## In progress
없음

## Next steps
0. ~~`/phase-run 13`~~ — **2026-09-24 완료.** 다음은 Current goal의 1→2→3 순서
1. **Phase 10 후속 (남은 것, 블로킹 아님)**:
   - **Step 2 트리 숫자가 컬럼 role 재지정 후 갱신되지 않는다** — `inspectDetailSheet`가 draft를 받지 않기 때문이다(§9 시그니처). 실제 반영값은 Step 4가 정확히 낸다
   - Step 3이 명부를 `getTeam`으로 따로 읽는다 — 미리보기 페이로드에 실으면 왕복이 준다(§9 변경 필요)
2. ~~**`guard.ts`의 표식 예외 처리 미검증**~~ — **2026-08-12 해소.** `npm run test:destructive` 7/7 통과로 실행 경로가 실제로 돌았다
3. **`prebuild`가 항상 `.next`를 비운다**(cache는 보존) — 빠른 반복 빌드가 필요해지면 `build`/`build:desktop` 분리를 검토한다. 그러려면 `tauri.conf.json`의 `beforeBuildCommand`를 바꿔야 한다
4. **배포는 NSIS setup.exe만 올린다** — SOT §14.1에 명문화. MSI도 함께 생성되지만 `perMachine`이라 관리자 권한을 요구한다(§12 위반). 공유 폴더에 둘 다 두면 사용자가 잘못된 쪽을 집는다
4. **남은 수동 검증** — 아래 "수동 검증 > 대기". **개발이 끝난 뒤 한 번에 진행한다**(2026-08-08 결정, 2026-08-10 재확인). 인쇄 3종 A4 미리보기는 §12 검증표상 **원래부터 사람 몫**이다
5. **다음 작업 (사용자가 정한 순서)**:
   - ~~제출 서식 엑셀 내보내기(§13 14번)~~ — **2026-08-12 Phase 11로 해소.** 엑셀 → 앱 → 엑셀 왕복이 닫혔다
   - ~~① HR API 연동 = Phase 12~~ — **2026-09-24 완료.** 키 등록 방법은 Current goal 참고
   - **② 부처별 규정**(§13 17번, 사용자 지시상 **마지막**) — PL-16에 따라 **출처가 확실한 표를 확보했을 때만**. 2026-08-12 사용자 확인: 나중에 조사해 넣고 **과제마다 다를 수 있으니 과제별로 규칙을 적용할 수 있어야 한다.** ⓐ 프리셋은 언제나 **기본값 제안**이고 과제별 입력이 이긴다(같은 부처 안에서도 기관 유형·공고마다 다르다 — 부처를 고른 순간 값이 잠기면 안 된다) ⓑ 지금 과제별로 담을 수 있는 규칙은 **비율 두 개뿐**(`allowanceRateLimit`·`indirectRateLimit`)이다. 조사 결과가 그보다 많으면(인건비 현물 비율 상한·장비비 단건 한도·연구활동비 세목별 상한 등) `Project`에 필드를 늘리지 말고 **규칙을 데이터로 담는 구조**로 바꾼다. **실제 규정 모양을 보고 정한다 — 지금 지어내지 않는다**
6. **SOT §9 조회 블록 이름 정리 (문서 부채, 블로킹 아님)**:
   - Phase 1~6에 걸쳐 조회 함수 이름이 §9 설계 목록과 갈렸다. `getProjectOverview`는 아예 구현되지 않았고(과제 개요가 6개 조회를 `Promise.all`), `getNotes(projectId, filter)`는 `getNotesData(projectId)`이며 필터는 화면 몫이다. `getMilestonesData`·`getTeamScreenData`·`getProjectsSummary`는 목록에 없다
   - 지금 개명하면 6개 Phase의 코드를 건드려야 해서 **SOT §9에 드리프트 경고를 달아 두고** 넘겼다. 한가할 때 §9를 실제 이름으로 맞출 것
7. **Phase 6 후속 (블로킹 아님)**:
   - 노트 편집이 WBS·마일스톤 화면에 **실시간으로 전파되지 않는다** — 그 화면들은 §8.5 구독표대로 `tasks`/`years`/`milestones`만 구독한다. 역참조는 다음 이동·`revalidatePath` 때 갱신된다. 즉시 전파가 필요하면 §8.5 구독표를 먼저 고쳐야 한다
   - 과제 개요의 고위험 리스크·최근 노트 카드도 같은 이유로 실시간이 아니다(§8.5 "화면당 최대 4테이블" 예산). 요약 카드라 현재는 충분하다고 판단
8. **Phase 5.5 후속 (블로킹 아님)**:
   - **`restore_import_snapshot`을 감싸는 서버 액션이 없다.** RPC와 리포지토리는 있고 통합 테스트도 리포지토리를 직접 부른다. §7.14 설정 화면(Phase 8)에서 액션을 만들 때 `tests/integration/import-actions.test.ts`의 복원 부분도 액션 경유로 바꿀 것
   - CSV는 SheetJS가 UTF-8로 가정한다 — **CP949 CSV는 깨진다.** 실측 서식이 전부 xlsx라 v1에서는 두었다
   - `orientation === 'column'`(전치 서식)은 명시적 거부 상태다. 실제로 그런 서식이 오면 `parseMatrix` 확장 필요
9. **연구비 후속 정리 (블로킹 아님)**:
   - `getBudgetMatrix`가 집계만 반환해 화면이 `listBudgetItemsByProject`로 원본 행(itemId·version·cash/inKind null 여부·executions)을 한 번 더 조회한다. `BudgetMatrixData`에 `items` 추가로 흡수 권장 (`app/projects/[id]/budget/page.tsx` 주석에 명시). **Phase 9에서 `getBudgetPlanData`를 만들 때 같이 정리하기 좋다** — 어차피 `detailCount`를 실어야 한다
   - `listBudgetItemsByProject`의 **부모** 조회는 무페이징 — 연차 84개 초과 과제에서 같은 절단이 발생한다(현실 규모에선 도달 안 함)
10. Phase 1 후속 개선(블로킹 아님): ① 인라인 이름 편집 중 Enter 연타 시 in-flight 재입력이 제출값으로 되돌아감 ② 자기 저장에 대한 가짜 STALE 배너(`disabled={saving}` 가드) — **노트 에디터에서는 이미 해결했으니 그 방식을 옮기면 된다** ③ `bulkUpdateTasks` 부분 반영(트랜잭션 RPC화 검토)
11. 미뤄둔 것: 단일 과제 조회 액션(`getProject(id)`)
12. 남은 관찰 항목(블로킹 아님): `tests/integration/wbs-queries.test.ts`는 팀 공유 설정 `app_settings.progress_weight_basis`를 잠시 바꿨다 되돌린다. 데이터를 지우지 않아 기본 실행에 두었지만, **두 PC에서 `npm test`를 동시에 돌리면** 서로의 임시값을 원본으로 착각해 설정이 어긋날 수 있다. 테스트는 한 번에 한 PC에서만 돌린다

## 부처별 규정 자료 (2026-09-24 사용자가 모음 — 더 모을 예정)
로컬 `부처별 연구비 사용 규정/`에 있다. **gitignore 대상**(공개 문서지만 26MB+라 저장소에 싣지 않는다). 다른 PC에서는 아래 목록으로 다시 받는다.
- `국가연구개발사업 연구개발비 사용 기준(과학기술정보통신부고시)(제2026-38호)(20260506).pdf` — **공통 기준의 원본.** 비율·한도는 여기서 시작한다
- `03. (협약관리) 국가연구개발사업 연구관리 표준매뉴얼(부처 및 전문기관 실무자용).pdf`
- `26년도_7월_국가연구개발혁신법_매뉴얼(본권,부록및별권)/` — 본권, 부록(서식), 별권 1 학생인건비통합관리, 2 기술료, 3 제재처분, 4 연구시설·장비비 통합관리
- `기후부/` — 기후에너지환경부고시 제2026-29호(에너지기술개발사업 공통 운영요령), 제2026-31호(기술료), 제2026-33호(장비 관리요령)
- 부처별 규정 구현(§13 17번)은 **이 자료의 실제 모양을 보고** 구조를 정한다(Next steps 5-②). 아직 착수 지시 없음
- **첫 훑기(2026-09-24, 텍스트 추출로 비율·한도 조항만)** — 지금 `Project`의 비율 두 개(`allowanceRateLimit`·`indirectRateLimit`)로는 **못 담는다**는 것이 확인됐다. 규칙의 기준(분모)·형태가 제각각이다:
  - 과기부 고시 제2026-38호(공통): 연구수당 ≤ **수정인건비의 20%**(제26조), 1인 지급 ≤ 계상액의 70% / 위탁연구개발비 ≤ **직접비에서 위탁·국제공동·부담비를 뺀 금액의 40%**(제27조) / 영리기관 장비 현물 ≤ **구입가의 20%** + 구입 5년 이내 조건(제66조), 기술도입비 현물 ≤ 50%, SW 현물 ≤ 20%(제68조) / 연구혁신비 ≤ **연차 평균 5천만원(절대액)** / 총인건비계상률 월 100% 초과 금지(영리기관) / 보안수당 ≤ 개인 인건비의 3% / 간접비고시비율: 영리 10%·비영리 17%·대학 5%(기본값) / 능률성과급 ≤ 간접비의 10%
  - 기후부 고시 제2026-29호(에너지기술개발사업 공통 운영요령, 116쪽): 공통 기준 위에 **사업 고유 규칙**이 얹힌다 — 참여연구자 최소 인건비계상률 **10%**(미달분 회수) / 동시수행 과제 5개·책임자 3개 / 기관부담연구개발비 중 **현금 비율**이 기업 유형별(대기업 15%·중견 13%·중소 10% 이상, 별표) / 사업조정비 ≤ 정부지원금의 10% / 장비비통합관리 적립 ≤ 수정직접비의 5%(공통은 10%) / 3천만원 이상 장비는 심의 / 연구지원전문가 인건비 50% 현금 / 별표 6 항목별 회수 기준
  - 따라서 구조는 **규칙을 데이터로**: `{ 대상 비목(세목), 기준(분모: 수정인건비·직접비-제외항·구입가·정부지원금·절대액), 비율 또는 금액, 방향(≤/≥), 조건(기관 유형·구입 5년 이내 등), 출처(고시 번호·조문) }`. 프리셋(부처·사업)은 기본값 제안이고 과제별 입력이 이긴다(2026-08-12 결정). **어느 규칙을 앱이 판정할지(계산 가능한 것만)와 어느 것은 안내만 할지**를 설계 때 SOT에 먼저 적는다

## 수동 검증 (누적)
### 완료
- 구글 OAuth 콘솔 + Supabase provider, 브라우저 실로그인(첫 사용자 자동 승인), Tauri 실로그인 전체 왕복(시스템 브라우저→딥링크→키체인→홈), cargo check, 백업 내보내기 실동작
- 검증 중 수정한 결함 2건: opener URL 허용 목록 부재, Windows 자격 증명 관리자 2560바이트 제한(키체인 청크 분할)
- **2026-08-07 — 고위험 5건 브라우저 실검증 전부 통과** (Phase 8 착수 전, 결함 0건):
  ① 노트 마크다운 XSS 실렌더(`<script>`·`onerror`·`javascript:` 링크가 실행되지 않고 원문 표시) ② 임포트 마법사 5단계(시트 추천·병합 셀·단위 확인 게이트·미매핑 0건·총액 298,510,000) ③ 충돌 다이얼로그 입력 보존(O-3) ④ 간트 막대·칸반 카드 드래그 + 상위 작업 경계 거절 ⑤ R-4 배너(폼 연 채 외부 변경)
  - 이때 **dev DB가 완전히 비어 있어**(과제 0건) 검증할 데이터가 없었다. `supabase/seed.sql` + 보충 데이터(기관 2·인력 3·Task 날짜·담당자·마일스톤 3)를 시드 과제 하위 고정 UUID로 넣었다. 되돌리기: `delete from projects where id='aaaa0000-0000-4000-8000-000000000001'`
  - 승인된 사용자는 `sangraedo@unes.co.kr` 1명뿐이다(다른 계정은 `/pending`으로 간다)

### 대기 (Phase 8 이후로 미룬 나머지)
- Phase 1: ① 두 세션 동시 수정 시 STALE 비교 UI·입력 보존 ② 드래그 이동·Tab/Shift+Tab·깊이 10 초과 거절 문구 ③ 편집 중 R-4 배너 동작
- Phase 4: ① `/projects/[id]/milestones` 실화면 CRUD·상태 인라인 변경·결과 메모 ② 연차 생성 시 기본 마일스톤 체크박스(종료일 없으면 비활성) ③ `/` 대시보드 6개 영역 실렌더
- Phase 5: ① 셀 인라인 편집(Enter/Esc/blur)·잠금 셀 안내 ② 셀 클릭 → 집행 패널 → 추가/수정/삭제 후 매트릭스 갱신 ③ B-2 빨강·B-3 배지 실렌더 ④ STALE 시 ConflictDialog 입력값 보존
- **Phase 5.5 (마법사는 자동 검증이 안 되는 부분이 많다 — 실제 브라우저로 확인할 것)**:
  ① 툴바 [엑셀 가져오기] → 모달. Esc/×/취소로 닫으면 상태 폐기(다시 열면 Step 1부터)
  ② 10MB 초과 파일·`.pdf`를 올리면 **업로드 전에** 이유와 함께 거부
  ③ 실측 총괄표 업로드 → Step 2에서 `유엔이_총괄표`가 ★와 S-13 점수로 하이라이트. 헤더 행 클릭 지정 시 연차·라벨 열 재감지
  ④ 병합 셀이 그리드에 실제로 병합되어 보인다(행안부 `연구재료비`/`(G)`, 산자부 `C16:E16`)
  ⑤ Step 2→3 진행이 **금액 단위 확인 체크박스**로 막힌다 (I-10: 1000배 오류는 치명적이라 자동 확정 금지)
  ⑥ Step 3: 자동 추정 연차 드롭다운은 회색, 사용자가 바꾸면 파랑. 하나라도 `미지정`이면 다음 버튼 비활성 + S-5 사유 표시
  ⑦ Step 4 아이콘(✅/🔵/⚠️/❌)이 실제 파일에서 기대대로. 미매핑에 비목 지정 시 ❌ 해제, 건너뛰기로 행 제외
  ⑧ Step 5: `#REF!` 셀이 빨강 오류 행이 되고 반영 버튼 비활성 → 그 행을 건너뛰면 활성화. 덮어씀 행에 `기존 → 신규` 표시
  ⑨ 반영 → 토스트 + 매트릭스 갱신. 프로파일 저장 후 다시 열어 프로파일 선택 시 Step 4로 직행
  ⑩ 마법사를 연 채 다른 브라우저에서 예산 셀 수정 → 자동 새로고침 대신 "새 변경 있음" 배너 (R-4)
- **Phase 6 리스크**: ① 히트맵 셀 클릭 → 목록이 그 조합만 남고, 재클릭·필터 칩 ×로 해제 ② "해결·종료 항목 표시" 토글 시 **셀 개수와 목록 건수가 함께** 늘어남(어긋나면 안 됨) ③ 두 창 동시 편집 → ConflictDialog가 입력값을 보존한 채 항목별 비교(O-3) ④ 폼 열어 둔 채 다른 창에서 수정 시 입력이 안 날아감(R-4) ⑤ 상태를 `발생`으로 → "주의" 뱃지, `해결`로 → 등급 "판정 제외" 회색 + 기본 화면에서 사라짐 ⑥ 대시보드 "고위험 리스크"·"주의 필요" 숫자가 리스크 화면 요약과 일치 ⑦ 정렬 "수동 순서"에서 ↑↓ 후 새로고침해도 순서 유지 ⑧ 행 확장의 "WBS에서 보기"가 해당 연차 트리로 이동
- **Phase 6 노트**: ① 타자 후 3초 정지 → "저장됨 HH:MM:SS", 목록 제목·날짜·태그가 **커서 위치를 잃지 않고** 갱신 ② 두 세션 동일 노트 → 앰버 배너 + 입력 보존 + `최신 내용 사용`/`내 입력 유지` 양쪽 동작 ③ 본문에 `<script>alert(1)</script>`, `<img src=x onerror=alert(1)>`, `[클릭](javascript:alert(1))`을 붙여넣어 **아무것도 실행되지 않고 글자 그대로 보이는지**(AST 단위로는 테스트됐지만 실제 브라우저에서 한 번 볼 것) ④ 참석자 체크박스 → `- 참석자:` 줄이 제자리에서 갱신되고 **중복 생성되지 않음** ⑤ WBS 상세 패널·마일스톤 펼친 행의 관련 노트 링크가 해당 노트를 선택한 상태로 이동
- **Phase 7 간트**: ① 좌측 패널 행과 우측 막대의 세로 정렬(마일스톤이 여러 줄로 쌓일 때 특히) ② 월 스케일(1일=3px)에서 드래그 체감 — 조금만 움직여도 여러 날이 간다. 실사용에서 견딜 만한지 ③ 두 창에서 같은 막대를 끌어 STALE → "내 위치로 다시 저장"이 최신 version으로 저장되는지 ④ 자식 막대를 끈 뒤 새로고침하면 **부모 요약 막대가 따라 늘어나는지**(P-14~P-16) ⑤ 일 스케일 3년(≈26,000px) 가로 스크롤 성능
- **Phase 7 칸반·매트릭스**: ① 카드를 다른 컬럼으로 → 상태 변경, `done`으로 옮기면 새로고침 후 진척률 100%(P-1) ② 같은 상위 작업 아래 카드끼리 순서 유지 / **다른 상위 작업 사이로 끌면 거절 배너** ③ 담당자 스윔레인 순서와 미지정 레인 위치 ④ 목록 행을 셀로 드래그 → 중요도 변경, 📌 꺼진 작업은 긴급도 안내, 📌 켠 뒤엔 긴급도도 변경 ⑤ 셀 클릭 필터·완료 표시 토글에서 **셀 숫자와 목록 건수 일치** ⑥ **리스크 화면 히트맵이 이전과 똑같이 보이는지**(Matrix5x5 추출 후 — 자동 회귀는 액션 레벨까지만 검증했다)
- **Phase 8 To-Do** (`/todos`): ① 한 줄 빠른 추가 → Enter, 실패해도 입력이 안 지워지는지 ② 필터 4종 전환 시 건수 — 특히 **`오늘`이 지난 마감을 포함**하는지(T-D2), `(과제 없음)` 선택이 되는지 ③ 정렬을 마감일/우선순위로 바꾸면 **드래그가 비활성 + 안내**(T-D9) ④ `수동` 정렬에서 드래그 후 **필터를 풀어도 순서가 안 섞이는지**(T-D10 — 여기가 틀리면 조용히 어긋난다) ⑤ 체크박스 토글이 두 창 동시 조작에서도 그냥 통과하는지(O-2), 제목 편집은 STALE 다이얼로그가 뜨는지(O-1) ⑥ 대시보드 "오늘의 To-Do" 건수와 `/todos` `오늘` 건수의 **차이가 아카이브 과제 몫만큼인지**(T-D6 — 의도된 차이다)
- **Phase 8 설정** (`/settings`): ① 팀 설정 6필드 저장 → 대시보드 마감 임박 기준일·진척률 가중이 실제로 바뀌는지 ② 두 창 동시 저장 시 ConflictDialog 입력 보존 ③ 내 프로필 Member 연결 드롭다운에 **전 과제 인력이 `기관명 · 이름`으로** 나오는지 ④ 임포트 스냅샷 패널: 과제 선택 → 목록 → **2단계 확인**(`되돌리기` 입력) → 복원 후 예산 매트릭스가 스냅샷 시점으로 돌아가는지
- **Phase 9 예산 제안** (`/projects/[id]/budget`): ① `[제안 | 수행]` 토글 — 셀 표시가 `예산/집행/집행률` ↔ `예산/현금/현물`로 바뀌고 새로고침하면 `수행`으로 돌아가는지 ② 제안 모드 셀 클릭 → 산출근거 패널이 매트릭스 아래 전폭으로 열리고 `scrollIntoView`로 끌려오는지 ③ 인건비 세목에서 인력을 고르면 **직위·연봉이 회색으로 채워지고 편집이 안 되는지**, 연봉 미입력 인력을 고르면 경고 + 인력 화면 링크가 뜨는지 ④ 같은 인력을 **두 행**으로 넣을 수 있는지(참여율 구간 분리) ⑤ `quantity` 세목에서 인자 라벨을 바꾸고 `%` 체크를 켰다 껐을 때 금액이 따라 바뀌는지 ⑥ **금액 열이 읽기 전용**이고 조정액으로만 맞춰지는지 ⑦ 조정액을 크게 넣어 최종 금액을 음수로 만들면 **빨강 + 사유가 뜨되 저장은 되는지**(PL-5) ⑧ 행 드래그 정렬이 세목 안에서만 되는지 ⑨ 두 창에서 같은 행을 고쳐 STALE → **입력값이 보존된 채** ConflictDialog, `[최신 값 사용]` 동작 ⑩ 마지막 행을 지우면 **직전 합계가 남고** 자물쇠 이유가 바뀌는지(0으로 안 돌아가는지)
- **Phase 9 잠금·검증 줄**: ① 산출근거가 있는 셀이 **두 모드 모두**에서 자물쇠 + 인라인 편집 차단인지, title에 건수가 나오는지 ② 수행 모드 집행 패널에서 **계획액 저장 버튼은 감춰지고 집행 내역 추가/수정/삭제는 그대로 되는지**(여기가 막히면 회귀다) ③ 지침 검증 줄의 연구수당·간접비 비율이 부록 B.7 값과 맞는지, 한도가 비었을 때 **배지 없이 비율만** 나오는지 ④ 총괄표 임포트로만 넣은 연차에서 `현금/현물 비중`이 **"미입력 N원 — 비중을 낼 수 없습니다"**로 나오는지 ⑤ 과제 개요 지침 한도 카드에서 빈 값이 `0%`가 아니라 **"검사하지 않음"**으로 읽히는지
- **Phase 9 인력·임포트**: ① 인력 폼에 연봉·채용구분이 있고 **참여율 필드는 없는지** ② 연봉을 바꾸면 **"산출근거 N건의 금액이 함께 바뀝니다"** + 연차별 전후 금액 확인 대화상자, 영향 0건이면 확인 없이 저장 ③ 인건비 산출근거가 걸린 인력의 삭제 버튼이 막히고 `[연구비로 이동]`이 뜨는지, **비활성화는 되는지** ④ 산출근거가 있는 셀을 총괄표 엑셀로 덮으려 하면 Step 5에 **`🔒 잠김 N건`이 `건너뜀`과 별도로** 세어지고 반영 버튼은 **살아 있는지** ⑤ 반영 후 토스트에 잠김 건수가 나오는지
- **Phase 9 인쇄**: 제안 모드에서 인쇄하면 **매트릭스 + 지침 검증 줄만** 나가고 산출근거 패널은 빠지는지, 부제에 `제안 모드`가 찍히는지, 수행 모드 인쇄가 종전과 같은지
- **Phase 10 산출근거 임포트** (`/projects/[id]/budget` 제안 모드 → `[산출근거 가져오기]`): ① 버튼이 **제안 모드에만** 보이고 수행 모드엔 없는지 ② Step 1에서 연차 제안이 시트명에서 나오되 **확정은 사용자**인지, 10MB·확장자 거부가 업로드 전인지 ③ Step 2 감지 트리(섹션→비목→세목→행 수)가 실제 파일과 맞는지, **`⚠️ 확인 필요`가 눈에 띄는지** ④ **총괄표 시트를 고르면 §7.9.1로 안내하며 거부**되는지 ⑤ 컬럼 role 드롭다운으로 역할을 바꾸면 **파랑 + ✎**로 바뀌고 Step 4 금액이 실제로 달라지는지(자동 감지는 회색) ⑥ Step 3 성명 3선택 — 새 인력 생성 시 연봉·직위가 파일에서 채워지는지, **동명이인은 아무것도 선택 안 된 상태**인지, 파일≠명부 연봉이 나란히 뜨는지 ⑦ Step 4에서 `계산 40,320,000 → 파일 40,050,000 (조정 −270,000)`이 보이는지 ⑧ 축 셀렉트가 **합계 열만 있는 세목에만** 뜨고, 현금→현물로 바꾸면 셀 합계가 옮겨가는지 ⑨ 기존 산출근거가 있는 셀이 **기본 건너뜀**이고 `[기존 삭제 후 교체]`를 켜야 반영되는지 ⑩ 결정을 바꾼 뒤 **[다시 확인] 없이는 반영 버튼이 비활성**인지 ⑪ 반영 후 매트릭스가 갱신되고 설정 화면에 스냅샷이 뜨는지 ⑫ **그 스냅샷을 복원하면 산출근거가 되돌아가고 "인력은 명부에 남습니다" 안내가 뜨는지**, 총괄표 스냅샷 복원 문구는 종전과 같은지
- **Phase 11 제출 서식 내보내기** (`/projects/[id]/budget` 제안 모드 → `[제출 서식 내보내기]`): ① 버튼이 **제안 모드에만** 보이는지 ② 연차를 바꾸면 요약(행 수·합계 금액)이 다시 계산되는지, 템플릿이 하나뿐일 때 셀렉트가 **안 나오는지**(X-3) ③ 학생인건비·위탁연구개발비처럼 **서식에 표가 없는 비목**이 있는 과제에서 내보내기 버튼이 막히고 **어느 비목이 몇 행인지** 읽히는지 ④ 음수 금액·연봉 미입력·한도 초과가 있어도 **경고만 뜨고 내보내기는 되는지**(§7.9.4, 협의 중인 계획을 내보내야 한다) ⑤ 받은 파일을 **실제 엑셀로 열어** — 소계·합계가 재계산되어 앱 값과 맞는지, 참여율이 `64%`로 보이는지(1000%가 아니라), 서식·테두리·병합이 원본 그대로인지 ⑥ **인력구분 칸에 `기존인력`/`신규채용`이 보이는지**(템플릿에서 한 번 지웠다 복원한 자리다) ⑦ 총괄표에서 **2~4차년도 열도 값이 채워지고 서식이 1차년도와 같은지**, 통합관리비·연구실 안전관리비·24·25행이 **빈 칸으로 나가는지**(그리고 그 사실이 화면에 안내됐는지) ⑧ 그 파일을 §7.9.3으로 **다시 임포트**하면 원래 산출근거가 돌아오는지
- **Phase 12 사내 명부 연동** (`/settings` → 사내 명부 연동, `/projects/[id]/team` → `[사내 명부에서 추가]`): ① **Tauri 앱**에서 키 저장 → 앱 재시작 후에도 "등록됨 ••••XXXX"가 유지되는지(키체인), 브라우저 모드에서는 "이 창에서만 유지됩니다"가 뜨는지 ② [연결 확인]이 "연결 성공 — 38명"류로 인원 수를 보여 주는지, 키를 지우고 누르면 401 문구에 `INVALID_API_KEY`와 한국어 message가 **둘 다** 보이는지 ③ 키 없이 인력 화면 버튼을 누르면 안내 + `/settings` 링크가 뜨는지(버튼이 감춰지지 않는지) ④ 목록에서 `user_division`이 null인 사람이 본부 `—`로 보이고 **선택 가능**한지, 퇴사자가 배지만 달고 선택 가능한지, 이미 등록된 이메일이 회색·disabled로 **남아 있는지** ⑤ 2명을 골라 추가하면 HR-2 문장이 notice에 남고 생성된 행의 **연봉이 비어 있는지** ⑥ 같은 사람을 다시 열면 `이미 등록됨`으로 바뀌는지 ⑦ §8.7 백업 파일을 열어 키가 **없는지** ⑧ 모달을 닫았다 열면 네트워크 탭에 HR 호출이 다시 찍히는지(캐시 없음)
- **Phase 13 연구비 규칙** (`/projects/[id]/budget` 제안 모드): ① 툴바 `[연구비 규칙]`이 **제안 모드에만** 보이는지 ② 모달에서 `moe_energy_sme` [채우기] → "추가 17 · 갱신 0 · 유지 0", 다시 [덮어쓰기] → 2단계 확인 후 "유지 17" ③ 값을 101로 고치면 행 아래 범위 문구, 20으로 되돌리면 저장 ④ 다른 탭에서 같은 행을 고친 뒤 저장하면 ConflictDialog → 항목별 비교 ⑤ `gov_share_max` 셀렉트 75/67·"직접 입력" ⑥ 하단 검증 패널에 비율 표 7행이 규칙 없어도 보이고, `indirect_max` 없으면 "기본 분모(과기부 공통)" 표기 ⑦ 실측 과제(부록 B.7)에서 `allowance_min` info 1건, findings 클릭 → 매트릭스 연차 열 파랗게 / 행 항목 클릭 → 정확한 비목 셀의 산출근거 패널이 열리고 행이 강조 ⑧ 규칙을 끄면 findings에서 사라지고 비율 표엔 "꺼짐"으로 남는지 ⑨ 내보내기 전 확인에 규칙 경고가 severity 순으로 뜨고 **내보내기가 막히지 않는지** ⑩ 수행 모드로 바꾸면 패널·버튼·모달이 전부 사라지는지 ⑪ 인쇄 미리보기에서 비율 표 "상한 초과"·"근사" 글자가 흑백에서 남는지
- **Phase 14 도움말·따라하기**: ① 대시보드·과제 목록·과제 각 탭·To-Do·설정의 `?`가 `/help`의 **그 화면 절**로 스크롤·강조되는지 ② 도움말 본문에 파서가 못 읽어 원문(`**`·백틱)이 그대로 보이는 곳이 없는지 ③ `[따라하기]` → `[예제 과제 만들기]` → 개요로 이동, 과제 목록에 `예제` 배지 ④ ①~⑦이 데이터에 따라 자동 체크되고 탭 이동 시 그 단계가 펼쳐지는지, ⑧⑨만 체크박스 ⑤ 각 단계 `[이 화면으로]`·"자세히 → 도움말" 링크 ⑥ 개요 `[과제 삭제]` 2단계(과제명 불일치 시 비활성) → 삭제 후 드로어가 초기 상태로 ⑦ `[다시 보지 않기]` → 아이콘 축소 → 설정 "따라하기" `[따라하기 다시 열기]`로 복귀 ⑧ Tauri 앱(standalone)에서 도움말이 열리는지(content 번들 포함 확인) ⑨ 인쇄 미리보기에서 `?`·따라하기 버튼이 숨는지
- **Phase 16 조직원·인건비**: ① `/staff` 조직원 추가 → 급여 이력 추가(월급 3,000,000·퇴직금 체크) → 목록에 연봉 36,000,000·월급·"퇴직금 포함" 배지 ② 같은 이메일(대소문자 다르게) 재등록 → 충돌 문구 ③ 과제 인력 화면 `[연결]` → 자동 제안 → 연결 후 `[급여 반영]` → 미리보기(전후 금액) → 반영 → 연봉·배지·산출근거 금액 갱신 ④ 급여 이력을 바꿔도 과제 연봉이 **안 바뀌는지**, 상세 패널에 `다름` 배지 ⑤ 연봉을 손으로 고치면 배지가 "기록 없음"으로 ⑥ 두 과제에 60%·50% 잡고 참여율 탭에 110% 빨강 "초과" ⑦ 제안 모드 `[인건비]` 탭 인라인 편집 → 매트릭스 값 갱신, 인쇄 미리보기 ⑧ 조직원 삭제 → 과제 인력의 연봉·배지 유지, 연결만 해제 ⑨ 회사 PC pull 후 `db push`(schema 4) 게이트
- **Phase 17 입력 양식**: ① 제안 모드 `[입력 양식 내려받기]` → 파일명·인건비/사업비 시트·숨김 `_meta`(엑셀에서 시트 숨김 해제하면 보임) ② 인건비 시트에서 참여율을 바꾸면 금액 열 수식이 엑셀에서 같이 바뀌는지 ③ `[입력 양식 올리기]` → 미리보기 요약·비목 합계·경고, 반영 후 매트릭스·인건비 탭 갱신 ④ 다른 과제의 양식을 올리면 거부 ⑤ 참여율 120 → 반영 비활성 + 사유 ⑥ 수행 모드에 버튼 없음 ⑦ 반영 후 설정 → 임포트 스냅샷 목록에 남는지
- **TDS 디자인 토큰** (전 화면): ① 아무 화면에서 색이 빠진(투명/검정) 요소가 없는지 — 옛 팔레트가 남았다는 뜻(`design-tokens` 테스트가 코드는 잡지만 동적 클래스 조합은 못 잡는다) ② 주 버튼이 파란색(#3182f6), 보조 버튼이 테두리 없는 회색인지 ③ Pretendard가 적용됐는지(브라우저 개발자 도구 computed font) — Tauri 앱 오프라인에서도 ④ 상태 색(진행 파랑·완료 초록·지연 빨강·임박 주황·리스크 등급)이 부록 A.3 의미대로 구분되는지 ⑤ 인쇄 미리보기 흑백 대비
- **Phase 8 인쇄 A4 3종** (§12 검증표상 **원래부터 사람 몫**): 연구비·리스크는 **가로**, 기술목표는 **세로**로 뜨는지 / 다른 화면으로 이동 후 다시 인쇄할 때 **이전 가로 설정이 새지 않는지**(P-R1 style 주입 방식의 유일한 위험점) / 여러 페이지 표에서 **헤더 행이 2쪽에도 반복**되고 행이 안 쪼개지는지 / 머리말의 출력일이 **Asia/Seoul 오늘**인지 / 흑백 인쇄에서 집행률 초과가 `초과` 글자로 읽히는지
- 공통: 두 PC 동일 데이터 조회, 동료 첫 로그인 시 /pending→승인 흐름(두 번째 회사 계정 필요), Tauri 자동 백업 7일 경과 실동작

## Blockers
없음. (Supabase 일시정지는 Resume으로 해소 — Current goal의 주의 참고)

## 정리 중 드러난 것 (2026-08-10 후속 정리)
1. **`npm run lint`는 한 번도 돌아간 적이 없다.** `package.json`의 `lint: next lint`는 있는데 **ESLint 설정 파일이 없어**(`.eslintrc*`·`eslint.config.*` 부재) 실행하면 대화형 설정 프롬프트가 뜬다. 그동안 여러 작업이 "린트 통과"로 보고했지만 **실제로 검증된 적이 없다.** 타입 체크(`tsc`)와 테스트는 실제로 돌고 있어 안전망이 없는 상태는 아니다.
   - **결정 필요**: ① ESLint 설정을 넣는다(10개 Phase치 코드에서 경고가 쏟아질 수 있다 — 별도 작업으로 잡아야 한다) ② `lint` 스크립트를 지운다(할 수 없는 것을 할 수 있는 척하지 않는다) ③ 그대로 둔다
   - 어느 쪽이든 **"린트를 돌렸다"는 보고를 그대로 믿지 않는다**는 것만은 지금 확정한다
2. **제안 모드가 여전히 두 시점을 섞는다** (C4가 절반만 해소): 표는 `data.matrix`(수행 조회)로 그리고 셀의 `saved`·`detailCount`·잠금은 `plan.cells`(제안 조회)로 그린다. 두 조회 사이에 누가 저장하면 한 표가 두 시점을 보인다. 창이 밀리초 단위라 실무 영향은 작지만 같은 계열의 결함이다. 해소하려면 두 액션을 합쳐야 하는데 **SOT §9가 독립 2항목으로 둔 것**이라 결정이 필요하다
3. **`BudgetPlanData`의 미사용 필드** — `matrix`·`years`·`projectName`·`todayISO`·`currencyUnit`을 `BudgetScreen`이 전부 `data.*`로 대체해 쓰고 있어 이 화면에서 소비되지 않는다. 순수 전송 낭비

## 서식 소유자 결정 대기 (Phase 11 — 진행을 막지 않는다. 해당 칸은 비워서 나간다)
1. **총괄표 24·25행 — 라벨과 수식이 서로 다른 비목을 가리킨다.** 라벨은 `위탁연구개발비`인데 수식은 `산출근거!G33`(**국제공동**연구개발비)이다. 어느 쪽이 맞는지는 서식을 만든 쪽만 안다. 지금은 두 행을 비워 내보내고, 그래서 **국제공동 금액이 직접비 계·총액에는 잡히지만 총괄표에 보이는 줄이 없다** — 화면 안내가 "빠진 게 아니라 보이는 줄이 없다"를 명시한다
2. **총괄표 10·11행(학생 인건비) — 축을 판정할 수 없다.** 다른 비목은 `현금`/`현물` 라벨이 붙어 있는데 이 두 줄에는 없다. 위가 현금이라고 짐작하면 틀릴 수 있고 둘 다 쓰면 금액이 두 번 보인다. 비우고 `skipped(ambiguous)`로 알린다
3. **표준 서식의 산출근거에 표가 없는 비목 5종** — 학생인건비·위탁연구개발비·연구개발부담비·연구과제추진비·기타. 그 비목을 쓴 과제는 내보내기가 **거부**되며 어느 비목이 왜 자리가 없는지 밝힌다. 해당 부처 서식을 추가로 받으면 X-3대로 템플릿 파일 + 맵만 넣으면 된다

## 사용자 결정 대기 (Phase 9에서 쌓인 것 — 진행을 막지는 않는다)
1. **구 백업 파일이 복원 불가가 됐다.** `schema_version` 1 → 2로 올려서 §8.7 K-5의 버전 게이트가 구 파일을 거부한다. 올리지 않으면 게이트가 "호환"이라 판정한 파일을 테이블 목록 검사가 거부해 **사용자가 "테이블 데이터가 없습니다"라는 엉뚱한 메시지**를 보게 되므로 올리는 쪽이 옳다고 판단했다. 되돌릴 수 없는 성질이라 알린다 — 현재 dev DB에 실데이터가 없어 실질 영향은 없다
2. ~~산출근거 패널의 `비고` 열~~ — **2026-08-09 유지 확정** (사용자 결정). §7.9.2 컬럼 목록에는 없지만 `BudgetDetail.note`(§5.17)를 편집할 화면이 여기뿐이라 칸이 없으면 저장된 값에 손이 닿지 않는다
3. ~~dev DB 잔여물 정리~~ — **2026-08-09 완료** (사람이 직접 SQL 실행). 자동 정리는 안전 분류기가 막았고 우회하지 않았다
4. ~~`npm run test:destructive`~~ — **2026-08-09 실행, 7/7 통과**. K-7 전체 대체 복원이 `budget_details`까지 되살리는 것을 확인했다. 실행 중 **테스트 시드 결함 1건**을 잡아 고쳤다(아래)

## How to run
- 테스트: `npm test` — 단위(`tests/unit`) + 통합(`tests/integration`). 통합은 실제 dev DB를 쓰고(네트워크 필요) **자기가 만든 데이터만** 지운다. 안전하게 아무 때나 돌려도 된다
  - 실행 1회 `tests/global-setup.ts`가 **중단된 이전 실행의 잔여물을 먼저 청소**한다(`wbs-test+%` 중 2시간 이상 경과분 + 그 소유 행). 무엇을 몇 건 지웠는지 콘솔에 남긴다
  - `samples/`가 없으면 실측 검증(`tests/unit/import-samples.test.ts`, `tests/integration/import-actions.test.ts`)이 **안내를 출력하고** 건너뛴다. 조용히 통과하지 않는다
  - ⚠️ vitest 기본 리포터는 **통과한** 테스트의 `console.log/warn`을 감춘다(`--reporter=verbose`에서만 보인다). 테스트에서 사람이 꼭 봐야 할 안내는 `process.stderr.write`로 쓴다
- ⚠️ 파괴적 테스트: `npm run test:destructive` — `tests/destructive/`(§8.7 K-7 전체 대체 복원 검증)만 돌린다. **대상 26종 테이블의 전 행을 지웠다 백업 시점으로 되돌린다**(Phase 9에서 `budget_details` 추가). 최근 실행: 2026-08-09 **7/7 통과**. 사람이 명시적으로 부를 때만 실행되며, 시작 전 가드(`tests/destructive/guard.ts`)가 테스트 소유가 아닌 데이터(시드 고정 UUID·`wbs-test+%` 사용자 소유가 아닌 projects/todos/notes/import_profiles 행)를 발견하면 아무것도 건드리지 않고 거부한다
  - **실데이터를 입력하기 시작하면 이 명령을 dev DB에서 돌리지 않는다.** 백업 복원 로직을 고칠 때만 빈 전용 DB에서 실행한다
  - 중단된 테스트의 잔여물도 "남의 데이터"로 보고 거부한다 — 아래 잔여물 정리 후 다시 실행
- 타입: `npx tsc --noEmit` / 빌드: `npm run build`
- 개발 서버: `npm run dev` → http://localhost:3000 (로그인 후 /projects)
  - ⚠️ **dev 서버를 띄운 채로 `npm run build`·`npm run tauri:build`를 돌리지 않는다.** 셋 다 같은 `.next`를 쓴다. 프로덕션 빌드가 dev 서버의 웹팩 캐시(`.next/cache/webpack/client-development/*.pack.gz`)를 덮어써서 `layout.css`가 503으로 떨어지고, **화면이 스타일 없이 뜨면서 버튼의 onClick이 붙지 않는다.** 2026-08-07 세션에서 "로그인 페이지가 망가졌다"로 나타났는데 코드는 멀쩡했다
  - 빌드가 필요하면 **dev 서버를 먼저 멈춘다.** 빌드 후 화면을 다시 봐야 하면 `.next`를 지우고 새로 띄운다 — 프로덕션 산출물이 섞인 `.next` 위에서 dev를 재개하면 증상이 남는다. Tauri 사이드카는 `src-tauri/resources/`에 복사된 뒤라 `.next` 삭제와 무관하다
  - 증상이 보이면 코드부터 뒤지지 말고 **CSS 응답 코드를 먼저 본다**(503/404면 이 문제다)
  - ⚠️ **`npm test` 전체 실행은 dev DB의 시드 과제(`aaaa0000-…-0001`)를 지운다** — `repos-hierarchy`/`rls-approval`/`wbs-queries`가 `applySeed` 후 `removeSeed`를 하기 때문이며 결함이 아니다. 화면으로 검증하는 중이었다면 `supabase/seed.sql`을 다시 적용할 것
- Tauri: `npm run tauri:dev` (Rust 필요) / 빌드: `npm run tauri:build`
- DB 마이그레이션: supabase link 불가(CLI 로그인 없음) — 직결:
  `npx supabase db push --db-url "postgresql://postgres.oqdcvdmodnpxmosnuitz:<암호URL인코딩>@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"`
  (암호는 루트 `데이터베이스 비밀번호(gitignore).txt`, 리전 aws-1-ap-northeast-2 서울. 다른 마이그레이션이 먼저 적용돼 있으면 `--include-all` 필요)
- 테스트 직결 SQL: `.env.test.local`의 TEST_DATABASE_URL / 앱 키: `.env.local` (+`ALLOWED_EMAIL_DOMAIN=unes.co.kr`)
- 테스트 중단 시 잔여물: `wbs-test+%` 패턴 사용자를 auth.users에서 직결 삭제 + 남은 테스트 과제(`created_by is null`) 삭제
- 집 PC 첫 세팅: git pull → npm install → `winget install Rustlang.Rustup` + VS BuildTools → **`.env.local`·`.env.test.local`은 gitignore라 저장소에 없다 — 회사 PC에서 복사해 올 것**
  - **Rust 툴체인은 2026-08-07에 회사 PC에만 설치했다**(cargo 1.97.1 / rustc 1.97.1 / VS Build Tools 2022 VCTools). 집 PC에서 Tauri를 빌드하려면 거기서도 설치해야 한다: `winget install --id Microsoft.VisualStudio.2022.BuildTools -e --override "--quiet --wait --norestart --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"` 후 `winget install --id Rustlang.Rustup -e`. 첫 `npm run tauri:build`는 의존 크레이트 전체를 컴파일해 4분 남짓 걸린다(이후엔 증분)
  - **`samples/`(실제 예산 엑셀)도 gitignore라 저장소에 없다.** 없으면 Phase 5.5 실측 검증이 건너뛰어진다 — 임포트를 건드릴 때는 회사 PC에서 복사해 올 것
  - `xlsx`(SheetJS)는 npm 레지스트리가 아니라 **공식 CDN 타르볼**에서 받는다(`package.json`에 URL). 레지스트리판은 0.18.5에서 멈춰 있고 취약점 권고가 붙어 있다. **사내망에서 `cdn.sheetjs.com`이 막히면 `npm install`이 실패한다** — 그 경우 사내 미러나 vendoring을 검토할 것
