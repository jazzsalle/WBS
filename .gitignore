node_modules/
.next/
.env.local
.env*.local
# 회사↔집 PC 이동용으로 복사해 온 env 사본 (anon 키·DB 비밀번호 포함) — 절대 커밋 금지
env*.download

# 실제 예산 자료 — 절대 커밋 금지
samples/
*.xlsx
*.xls
!tests/fixtures/*.xlsx
# 제출 서식 내보내기용 빈 템플릿 (§7.9.4). 값을 전부 비우고 서식·병합·수식만 남긴 파일이며,
# 금액·성명이 하나도 없음을 기계로 검증한 뒤에만 넣는다. samples/ 원본과 혼동하지 말 것
!templates/*.xlsx

# 비밀번호·시크릿 메모 — 절대 커밋 금지
*(gitignore)*
데이터베이스 비밀번호(gitignore).txt
*URL_KEY*.txt

# 루트의 스크린샷 메모 (개인 작업 파일)
/화면캡쳐*.png

# 빌드 산출물
tsconfig.tsbuildinfo
next-env.d.ts

# Tauri
src-tauri/target/

# 백업
backups/

# 부처별 연구비 사용 규정 원문(공개 고시·매뉴얼 PDF, 26MB+, 계속 늘어남). 저장소에 싣지 않는다 —
# 목록·출처는 PROGRESS.md "부처별 규정 자료"에 적어 두고, 다른 PC에서는 그 목록으로 다시 받는다
/부처별 연구비 사용 규정/
