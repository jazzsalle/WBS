# R&D 과제 관리 도구

## 스펙

**`docs/SOT.md`가 이 프로젝트의 유일한 기준점이다.**

- 작업 시작 전 관련 섹션을 읽는다. 기억이나 추측으로 구현하지 않는다.
- 코드와 SOT가 다르면 **SOT가 옳다.** 코드를 고친다.
- 구현 중 SOT를 바꿔야 한다고 판단되면, **코드보다 SOT를 먼저 고치고** 무엇을 왜 바꿨는지 알린다.
- SOT에 없는 결정이 필요하면 임의로 정하지 말고 **먼저 물어본다.**

## 기술 스택

| | |
|---|---|
| 프레임워크 | Next.js 15 (App Router), TypeScript strict |
| 스타일 | Tailwind CSS v4 |
| DB | Supabase (PostgreSQL) |
| 인증 | Supabase Auth + Google OAuth |
| 배포 | Tauri v2 데스크톱 앱 (Next.js standalone 사이드카) |
| 검증 | Zod |
| 엑셀 | SheetJS (`xlsx`) — 서버 전용 |

## 절대 규칙

1. **`service_role` 키를 클라이언트나 빌드 산출물에 넣지 않는다.** anon 키 + 사용자 세션 + RLS만 쓴다. 데스크톱 앱은 사용자 손에 있는 코드다.
2. **모든 테이블에 RLS를 켠다.** 새 테이블을 만들면 같은 마이그레이션에 정책을 함께 쓴다. 예외 없다.
3. **UI와 서버 액션은 supabase 클라이언트를 직접 호출하지 않는다.** 반드시 `lib/db/`의 리포지토리를 거친다. 이 규칙이 나중의 NAS 셀프호스팅 이전을 가능하게 한다 (SOT §14.5). 유일한 예외: Realtime 구독(읽기 전용)은 클라이언트 컴포넌트에서 직접 한다 (SOT §8.2 C-2, §8.5).
4. **금액은 원 단위 정수로 저장한다.** 부동소수점 연산 금지. 표시할 때만 환산한다.
5. **에러를 조용히 삼키지 않는다.** 빈 배열 폴백 금지. 데이터 손상은 사용자에게 명시적으로 알린다.
6. 스키마 변경은 `supabase/migrations/`에만 쓴다. 대시보드에서 직접 테이블을 고치지 않는다.

## 순수 함수 + 테스트 필수

아래 모듈은 부수효과 없는 순수 함수로 만들고 **반드시 단위 테스트를 작성한다.** 여기가 틀리면 나머지 전부가 틀린다.

| 모듈 | 대상 | SOT |
|---|---|---|
| `lib/tree.ts` | 트리 구성, WBS 코드, 순환·깊이 검사 | §6.6, §6.7 |
| `lib/progress.ts` | 4단계 진척률 롤업 | §6.1 |
| `lib/goals.ts` | 성과목표·기술목표 달성률 | §6.2, §6.3 |
| `lib/budget.ts` | 집행률 | §6.4 |
| `lib/priority.ts` | 긴급도 자동 계산, 우선순위 점수 | §6.9 |
| `lib/risk.ts` | 리스크 등급 판정 | §6.5 |
| `lib/dates.ts` | 마감 판정, 날짜 롤업, 간트 좌표 | §6.5, §6.1.1 |
| `lib/import/` | 엑셀 파싱, 비목 매칭 | §6.8 |
| `lib/notes.ts` | 마크다운 파싱, URL 안전 판정, 정렬·필터 | §7.12 |
| `lib/gantt.ts` | 간트 좌표(차트 범위·x좌표·눈금·연차 밴드) | §7.5 |
| `lib/board.ts` | 칸반 컬럼·우선순위 5×5 셀 집계 | §7.6 |
| `lib/todos.ts` | To-Do 필터(T-D1~T-D9)·정렬·지연 판정 | §7.13 |

비목 별칭 사전·스킵 패턴·축 라벨은 SOT **부록 C**가 원본이고 `lib/constants.ts`에 정의한다. 임포트 관련 수정 시 부록 C를 먼저 본다.

**노트 본문은 사용자 입력이다.** `lib/notes.ts`는 마크다운을 **데이터 AST**로 파싱하고 뷰어가 그것을 React 엘리먼트로 옮긴다 — HTML 문자열을 만들지 않으므로 저장형 XSS가 구조적으로 불가능하다. **`dangerouslySetInnerHTML`을 노트 경로에 절대 도입하지 않는다.** 마크다운 기능을 넓힐 때도 AST 노드 종류를 늘리는 방식으로만 한다.

**SOT 부록 B의 계산 검증 예시를 그대로 테스트 케이스로 옮긴다.** 숫자가 안 맞으면 구현이 틀린 것이다.

§6.3 기술목표 달성률은 `direction`(higher/lower/exact) × `baselineDomestic` 유무 조합에서 실수가 잦다. 모든 분기를 테스트한다.

## Phase (구현 순서)

SOT §11을 따른다. 순서를 건너뛰지 않는다. Phase 실행은 `/phase-run N` (planner → generator 병렬 → evaluator). 합격 기준은 `evaluation_criteria.md`.

| Phase | 목표 | 핵심 산출물 | SOT |
|---|---|---|---|
| 0 | 기반 | Supabase 스키마+RLS 마이그레이션, 타입·Zod, 리포지토리 16종, 매퍼 | §5, §8, §14.3 |
| 0.5 | 셸 + 인증 | Tauri 셸, 구글 OAuth 딥링크, 사용자 승인, **백업/복원** | §14, §8.7, §7.0 |
| 1 | 계층 + WBS | 과제·단계·연차·Task CRUD, 트리 조작, 4단계 진척률 롤업, 우선순위 | §6.1, §6.6~6.9, §7.4 |
| 2 | 인력 + 기관 | 기관·인력 CRUD, 담당자 배정 | §5.10~5.11, §7.10 |
| 3 | 목표 관리 | 성과·기술목표 CRUD, 실적·측정, 달성률 | §6.2~6.3, §7.7 |
| 4 | 마일스톤 + 대시보드 | 마일스톤 CRUD·자동생성, 마감 판정, 대시보드 | §6.5, §7.2, §7.8 |
| 5 | 연구비 | 비목 매트릭스, 예산 편집(현금/현물), 집행 등록, 집행률 | §6.4, §7.9 |
| 5.5 | 엑셀 임포트 | 파싱 파이프라인, 5단계 마법사, 프로파일, 스냅샷 | §6.8, §7.9.1, 부록 C |
| 6 | 리스크 + 노트 | 리스크 대장·매트릭스, 마크다운 노트 | §5.13~5.14, §7.11~7.12 |
| 7 | 간트 + 칸반 | 간트(연차 밴드·마일스톤 레인), 칸반·우선순위 매트릭스 | §7.5~7.6 |
| 8 | To-Do + 설정 + 마감 | To-Do, 설정 화면, 인쇄 레이아웃, 비기능 검증 | §7.13~7.15, §12 |

Phase 0.5의 백업(§8.7)을 미루지 않는다. 무료 플랜에는 DB 백업이 없어서 이게 유일한 안전망이다.

## 민감 파일

```
samples/          실제 예산 엑셀. .gitignore 필수. 절대 커밋 금지
.env.local        Supabase URL, anon 키
```

익명화된 테스트 픽스처만 `tests/fixtures/`에 커밋한다.

## 코드 컨벤션

- DB는 `snake_case`, 앱은 `camelCase`. 변환은 `lib/db/mapper.ts`가 전담한다.
- 서버 액션 반환은 `ActionResult<T>`. 예외를 그대로 던지지 않는다.
- 클라이언트 컴포넌트는 인터랙션이 필요한 곳(드래그, 폼, 모달)에만 쓴다. 데이터 로딩은 서버에서.
- 파생 값(WBS 코드, 진척률, 달성률)은 **저장하지 않는다.** 읽을 때 계산한다.
- 주석은 "왜"를 쓴다. "무엇"은 코드가 말한다.

## 작업 방식

- 큰 변경은 계획을 먼저 보여주고 승인을 받는다.
- 한 번에 한 Phase씩. 여러 Phase를 동시에 진행하지 않는다.
- 테스트가 깨진 채로 다음으로 넘어가지 않는다.
- 확신이 없으면 추측하지 말고 묻는다.
