node_modules/
.next/
.env.local
.env*.local
# 회사↔집 PC 이동용으로 복사해 온 env 사본 (anon 키·DB 비밀번호 포함) — 절대 커밋 금지
env*.download

# 실제 예산 자료 — 절대 커밋 금지
samples/
*.xlsx
*.xls
!tests/fixtures/*.xlsx

# 비밀번호·시크릿 메모 — 절대 커밋 금지
*(gitignore)*
데이터베이스 비밀번호(gitignore).txt
*URL_KEY*.txt

# 루트의 스크린샷 메모 (개인 작업 파일)
/화면캡쳐*.png

# 빌드 산출물
tsconfig.tsbuildinfo
next-env.d.ts

# Tauri
src-tauri/target/

# 백업
backups/
